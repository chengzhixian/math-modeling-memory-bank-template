% Owner: cyj. Only cyj edits this file; zhh integrates it through main.tex.
\section{问题二：广义标度律}
本节预留给 cyj 的正式模型、参数、附件 B 验证和 Q/p 桥接检验。
当前不能将附件 A 的 $Q_z$ 与附件 B 的 $Q_{\mathrm{score}}$ 直接等同，也不能默认附件 A 的任一验证域 Loss 与 B1 的 \texttt{val\_loss} 同尺度。
% TODO(cyj): 写入正式章节后删除本段占位说明。
