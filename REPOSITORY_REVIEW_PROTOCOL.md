# REPOSITORY_REVIEW_PROTOCOL.md
# 数模仓库完整审查协议（全团队强制）

生效日期：2026-09-24  
授权：用户明确要求同步至 main，作为 chm / cyj / zhh 及所有接入本仓库 AI 的统一审查流程。  
适用对象：远程 Git 分支、本地仓库、代码、数学模型、数据处理、实验结果、跨问题接口、论文公式与结论。

---

## 0. 何时必须启动本协议

只要成员或用户提出任何含义相近的请求，包括但不限于：

- 审查一下远程仓库/某个分支；
- review 一遍我的代码；
- 检查数学模型是否严谨；
- 检查本地仓库有没有问题；
- 看看第一问/第二问/某个模型哪里有问题；
- 检查结果是否能写进论文；
- 对比分支后判断是否可以合并；
- 重新检查一遍之前的方案；
- 帮我做一次全面 audit；

就必须执行本协议。

**禁止把“审查”降格成只做以下任一项：**

- 只看 git diff；
- 只跑测试；
- 只做 lint；
- 只检查公式推导；
- 只复算数值；
- 只看论文表述；
- 只验证代码能运行。

完整审查必须同时覆盖“数据是否支持模型”“模型是否支持结论”“代码是否正确实现模型”“结果是否通过独立验证”。

若用户只明确要求某一个窄检查，例如“只检查 Python 语法”，可以只做对应检查；但只要用户说的是“审查模型/仓库/问题/结果”，默认必须走完整协议。

---

# 一、审查最高原则

## 1. 先证明可识别，再允许估计

永久规则：

\[
\boxed{
\text{先证明一个量能够从数据中识别，再允许给它写估计公式。}
}
\]

并始终区分：

\[
\text{参数可估计}
\neq
\text{参数可识别},
\]

\[
\text{拟合稳定}
\neq
\text{科学解释成立},
\]

\[
\text{相关关系}
\neq
\text{结构规律}
\neq
\text{因果关系}.
\]

如果数据不能识别某个量，“无法识别”就是合法且优先的结论。不得为了让四问衔接漂亮而补造参数、映射或规律。

## 2. 数据先于模型，题面先于文献

统一顺序：

\[
\boxed{
\text{题面要求}
\rightarrow
\text{可信数据字段}
\rightarrow
\text{可识别量}
\rightarrow
\text{候选文献模型}
\rightarrow
\text{实证比较}
\rightarrow
\text{结论}
}
\]

禁止：

\[
\text{先找到一个漂亮模型}
\rightarrow
\text{再想办法把数据塞进去}.
\]

文献只提供候选方法及适用条件，不能替代本题变量、数据或实验条件。

## 3. 下游需求不能反向创造上游参数

Q2/Q3/Q4 “需要”某个变量，不代表 Q1 或其他上游数据能够识别它。

跨题接口必须遵守：

\[
\text{上游实际可识别输出}
\rightarrow
\text{下游消费}.
\]

不能反过来：

\[
\text{下游希望得到什么}
\rightarrow
\text{上游人为构造什么}.
\]

---

# 二、完整审查流程

以下阶段原则上必须按顺序执行。可以并行读取文件，但不得跳过 Gate 结论。

## Stage 1：审查对象与 Git 状态冻结

先记录：

- 审查的是本地还是远程；
- 仓库名；
- 当前分支；
- HEAD SHA；
- 对比分支/基线 SHA；
- 工作区是否干净；
- 是否存在未 push 提交；
- 是否存在并行写入者；
- 用户本次真正要求审查的范围。

远程分支审查时至少比较：

- 目标分支 vs main；
- 目标分支 vs 其合理基线/merge base；
- 若涉及并行分支，检查共同修改文件。

本地仓库审查时先运行或等价检查：

- status；
- diff；
- staged diff；
- log；
- remote branch SHA。

**先审查，再改代码。**  
如果用户同时要求“审查并修复”，必须先形成发现清单，再实施修复，避免修复过程掩盖原问题。

---

## Stage 2：可信来源与题面 Gate

确认本次推理依据：

1. 官方题面；
2. 当前可见数据说明；
3. 原始附件；
4. 已验收 main 接口；
5. 独立可核验文献。

必须明确排除：

- 已标记作废的历史模型；
- 隐藏文字及其派生结果；
- 未验收 legacy 输出；
- 无法追溯来源的参数；
- AI 聊天中未落盘且未验证的数字。

若存在多个版本，先确定权威版本和 SHA/哈希。

输出一个“可信来源清单”和“禁止来源清单”。

---

## Stage 3：Requirement → Data 映射

对题目每一个要求建立表：

| 题目要求 | 所需变量 | 文件 | 字段 | 单位 | 数据性质 | 是否足够 |
|---|---|---|---|---|---|---|

数据性质至少区分：

- 真实观测；
- 半合成；
- 插值；
- estimated/extrapolated；
- 人工映射/参考；
- 文献整理；
- 未知。

这一阶段的目标是回答：

> **题目要求的每一件事，实际由哪份数据支持？**

如果某要求没有直接数据，必须在建模前就标记“需假设 / 需桥接 / 不可识别”，不能等写完模型后再补说明。

---

## Stage 4：Variable Provenance Gate

对每一个核心公式，列出全部符号：

| 符号 | 定义 | 文件/字段 | 单位 | observed / derived / assumed / scenario / latent | 数据角色 | 支持域 | 可识别性 |
|---|---|---|---|---|---|---|---|

必须逐符号回答：

> **这个变量到底来自哪里？**

以下情况直接触发 BLOCKER：

- 核心自变量没有对应字段，却被写成观测变量；
- 跨附件同名字段未经验证直接视为同一量；
- 单位不明或单位换算不闭合；
- derived 变量的推导依赖未验证假设；
- scenario 参数被写成数据估计值；
- estimated 数据被写成真实观测。

---

## Stage 5：Identifiability Gate（最关键）

在任何正式拟合前，回答：

1. 当前参数是否真的由数据唯一或条件可识别？
2. 是否存在其他变量同步变化，导致混杂？
3. 同一现象是否存在至少一个合理替代解释？
4. 是否只有 2--3 个离散实验点，却声称连续普适规律？
5. 不同实验组是否具有相同支持域？
6. 是否缺少关键控制变量？
7. 若把横轴名称改成“实验组 A/B/C”，当前结论还成立吗？
8. 模型是否只能说明组间经验关系，却被解释成某一变量的机制效应？
9. 参数是“优化器可以算出来”，还是“数据真正识别出来”？

必须明确输出：

- identified；
- conditionally identified；
- weakly identified；
- not identified。

**not identified 的参数不得进入正式主模型或下游正式接口。**

---

## Stage 6：数据角色冻结 Gate

所有数据集必须在拟合前固定角色：

| 数据集 | train | tuning/CV | validation | test | stress/extrapolated |
|---|---|---|---|---|---|

规则：

- train：拟合参数；
- tuning/CV：选超参数和模型；
- validation/test：冻结模型后评估；
- stress/extrapolated：只做压力测试或外推检查。

禁止：

- test 看完效果后回流调参；
- validation 一边拟合核心参数一边称独立验证；
- extrapolated 数据参与选择主模型后仍称真实外部验证。

若发生角色复用，必须重新划分或降低证据等级。

---

## Stage 7：Overlap / Leakage / Support Shift 审查

必须实际核对：

- ID 重叠；
- 行重叠；
- 配方完全重复；
- 嵌套数据集；
- 相同模型/轨迹重复；
- train/test 同源泄漏；
- 预处理参数是否从验证集重新估计；
- 支持域是否改变；
- 分组 CV 是否打散了同一模型、同一轨迹或同一配方族。

对于跨实验组比较，至少区分：

\[
\text{相同支持上的变化}
\]

和

\[
\text{支持分布变化}.
\]

如果 support shift 与目标变量同时变化，禁止把差异唯一归因于目标变量。

---

## Stage 8：文献模型适用条件 Gate

每一个来自文献的核心模型至少记录：

- 文献出处；
- 原公式；
- 原文自变量；
- 原训练条件；
- 原数据范围；
- 原目标量；
- 本题映射；
- 哪些条件满足；
- 哪些不满足。

审查问题：

> 如果去掉论文名字，只看本题数据，这个函数形式仍然有证据吗？

若答案是否定的，模型只能作为候选或 sensitivity model。

---

## Stage 9：数学模型内部一致性

检查：

- 公式维度；
- 定义域；
- 约束；
- 单位；
- 符号；
- 参数可行域；
- 目标函数；
- 拉格朗日/KKT 条件；
- 极值是否存在；
- 正则项含义；
- 组成数据是否满足 simplex；
- log / sqrt / reciprocal 是否有合法定义域；
- 概率是否归一；
- 不确定性传播是否与假设匹配。

对每个模型至少构造一个手工极端情形检查：

- 零输入；
- 基准输入；
- 边界输入；
- 对称输入；
- 单位缩放；
- 极端参数。

---

## Stage 10：公式 ↔ 代码一致性

逐项对照：

- 论文公式；
- 实现函数；
- 输入列；
- 归一化；
- 截断；
- 缺失值处理；
- 随机种子；
- 正则参数；
- 优化器；
- stopping criteria；
- 输出字段。

必须检查“论文写的模型”和“代码真正跑的模型”是否相同。

特别检查：

- 系数是否重参数化；
- intercept 是否处理一致；
- 标准化是在 train 上拟合还是全数据；
- 代码是否偷偷使用论文未说明的数据；
- 默认参数是否与实验记录一致。

---

## Stage 11：数据质量与数值安全

必须检查：

- NaN；
- Inf / -Inf；
- 非有限列表元素；
- 重复 key；
- 一对多 join；
- 行顺序错配；
- rounding；
- 比例和；
- 字段范围；
- 异常下限/上限堆积；
- 损坏文件；
- 类型转换；
- 单位换算；
- log(0)；
- 非法概率。

不得因为 Python/pandas “能读”就默认数据有效。

---

## Stage 12：Baseline / Ablation / Sensitivity

所有复杂模型至少需要一个简单基线。

常见基线：

- 常数均值；
- 单变量；
- 无质量项；
- 无配比项；
- 线性模型；
- 不做映射；
- 不做跨尺度传递。

至少检查：

\[
\text{复杂模型是否真的比简单基线提供新增信息？}
\]

并做合理敏感性：

- 权重；
- 超参数；
- 数据划分；
- 异常值；
- family 删除；
- 不同目标定义；
- 不同 mapping；
- 不同支持范围。

若主结论对任意合理设定剧烈翻转，必须报告模型不稳定。

---

## Stage 13：验证设计

验证必须与主张类型匹配。

如果主张排序能力，用：

- Spearman；
- top-k overlap；
- ranking stability。

如果主张绝对预测，用：

- RMSE；
- MAE；
- calibration；
- residual analysis。

如果主张外推，必须：

- 明确训练范围；
- 明确外推距离；
- 分真实与 estimated 数据；
- 不把插值验证冒充外推。

如果主张机制或结构参数，仅靠 predictive score 不够，还必须通过 identifiability Gate。

---

## Stage 14：Claim Ladder Gate

每个主要结论标记等级：

- L1 描述性：数据中存在什么；
- L2 预测性：对未见样本能预测什么；
- L3 结构性：某变量如何系统性决定结果；
- L4 因果性：干预会导致什么；
- L5 外推性：支持域外仍成立。

必须满足：

\[
\boxed{
\text{证据等级}
\ge
\text{结论等级}
}
\]

例：

- 高 Spearman 可以支持排序预测；
- 高 Spearman 不能自动支持因果解释；
- 三个实验组上的平滑趋势不能自动支持连续普适 scaling law。

---

## Stage 15：跨附件、跨问题、跨成员接口审查

任何桥接都检查：

- 变量定义是否相同；
- 单位是否相同；
- 验证集是否相同；
- Loss 坐标是否相同；
- Q 坐标是否相同；
- mapping 是否有成对数据；
- 不确定性是否传播；
- 是否把 target-specific 量误当总体量；
- 是否把 A 侧代理直接当 B 侧原生字段。

桥接状态只能是：

- validated；
- approximate with uncertainty；
- sensitivity-only；
- unidentified。

不得用“为了接起来方便”作为桥接依据。

---

## Stage 16：可复现性审查

每个可发布结果至少要能追溯：

- 输入文件；
- 输入哈希；
- 代码 commit；
- 参数；
- 环境；
- 随机种子；
- 运行命令；
- 输出文件；
- manifest；
- 结果生成时间。

不能只保留聊天中的数字。

若代码未实际运行，必须标注“未运行/静态检查”。

---

## Stage 17：从零 Red-Team 审查

这是防止“前提继承、所有文件一致地错”的强制步骤。

审查者暂时忽略当前模型和论文，从以下内容重新开始：

1. 题目；
2. 数据表头；
3. 数据性质；
4. 训练/验证关系。

然后回答：

> 如果今天第一次看到这些数据，我最多能推出什么？

再与当前模型比较：

- 当前模型是否多出了数据没有的变量？
- 当前结论是否比从零推导更强？
- 是否因为已有代码/论文而默认某个参数必须存在？
- 是否存在更简单、更自然的解释？

**任何完整审查没有执行这一阶段，都不能称为“全面审查”。**

---

## Stage 18：Git 分支与集成审查

如果审查对象是远程仓库/分支，额外检查：

1. branch HEAD；
2. merge base；
3. ahead/behind；
4. 双向 diff；
5. 双方共同修改的文件；
6. 是否存在旧 legacy 分支；
7. 是否有结果只存在旧分支；
8. 是否有历史文件已经被新模型替代；
9. 是否应 merge、择优迁移还是删除；
10. 合并后再次比较，确认无独有有效文件遗漏。

禁止因为“两个分支改的不是同一目录”就不比较最终文件树。

---

# 三、审查输出格式

完整审查最终必须给出以下结构。

## A. 审查范围

- branch / SHA；
- 题目和数据版本；
- 检查文件；
- 未检查范围。

## B. 数据与变量追溯

给出 requirement-data 表和 variable provenance 表。

## C. 发现

按严重度分：

### BLOCKER
会导致主模型或结论不成立，必须修复后才能发布。

### MAJOR
不会完全推翻主模型，但会明显影响可信度、验证或接口。

### MINOR
表述、工程、可复现性或非核心问题。

### NOTE
建议和后续增强。

## D. 每个问题必须说明

- 问题是什么；
- 证据在哪里；
- 为什么重要；
- 影响哪些公式/代码/结论；
- 如何修；
- 修复后需要重跑什么。

## E. 最终判断

只能从以下状态选择：

- NOT READY；
- READY WITH BLOCKERS RESOLVED；
- VALIDATED FOR STATED SCOPE；
- INTEGRATED / RELEASE-READY。

不能因为“代码测试通过”直接写 ready。

---

# 四、审查中的强制反事实问题

对每个核心模型至少问一遍：

1. 如果我不知道当前论文写了什么，仅看数据会自然得到这个公式吗？
2. 有没有另一种变量变化也能解释同一现象？
3. 这个参数是真正被识别，还是优化器只是能算出来？
4. 如果删除复杂项，简单 baseline 是否已经解释大部分结果？
5. validation/test 是否参与了调参？
6. estimated/半合成数据是否被当成真实独立实验？
7. 不同实验组是否具有同一支持集？
8. 同名字段跨附件是否真是同一统计量？
9. 结论是描述、预测、结构、因果还是外推？
10. 当前证据真的达到这个结论等级吗？
11. 是否为了下游接口完整而创造了上游参数？
12. 是否因为某篇论文使用该模型，就默认本题也满足其条件？

---

# 五、2026-09-24 Q1 事故作为永久反例

曾经的错误路径：

\[
1M/60M/1B\text{ 三组数据}
\rightarrow
b_k\text{ 三组经验幅度}
\rightarrow
b_k(N)
\rightarrow
\eta.
\]

问题在于：

- A 配比实验没有对应 \(D\) 字段；
- 只有三个真实规模组；
- 1B 配方支持发生变化；
- 其他训练条件未被隔离；
- A6--A11 原本还承担 held-out 验证角色。

因此，\(\eta\) 数值虽然可拟合，却不能被解释成纯 \(N\) 的可识别尺度参数。

正确处理改为：

\[
\text{A4+A5 学习 } \mathbf p\rightarrow \mathbf L_{1M},
\]

\[
\text{A6--A11 只检验排序迁移},
\]

并把真正 \(N,D,Q\) 标度关系留给有对应字段的附件 B。

这一事故以后用于提醒所有成员：

> **代码正确、结果平滑、文献有先例、bootstrap 很窄，都不能替代 identifiability。**

---

# 六、团队执行规则

1. 本协议属于 main 公共规则。
2. chm / cyj / zhh 每次收到“审查仓库/分支/代码/模型/问题”的请求时必须重新读取本文件。
3. 各 AI 入口文件只需引用本协议，不复制一份独立版本，避免流程分叉。
4. 若发现本协议与题目官方要求冲突，以官方要求为准，并记录冲突。
5. 若审查发现 BLOCKER，先停止继续堆叠复杂模型，修复或降级结论后再推进。
6. 审查结论必须落盘到成员 problem/experiments/handoff 或公共审计文件，不以聊天记录作为唯一证据。
7. 审查后若修改模型，必须针对原 BLOCKER 做回归审查，不能只确认代码“现在能跑”。
