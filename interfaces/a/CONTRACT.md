# A 交付约定 草案 v1

生产者 A；使用者 B（Q2）、A（Q3）、C（论文/不确定性）。09-23 18:00 前与使用者确认；当前无结果。

建议交付：
- domain_quality.csv：领域体系与 domain_id、样本数、数据集标识、质量值 Q、评分版本、缺失/不确定性说明。抽样、扩展及全量按可比较口径标识，不能混写。
- domain_mapping.csv：来源/目标领域、映射依据、权重定义和未映射标记；17 域顺序单独说明。
- mixture_effect 文件及说明：实验 ID、17 域比例、观测/预测 Loss、训练/检验/外推划分、尺度与参考配方；具体模型和格式待团队决定。
- optimization 文件及说明（后续 Q3）：预算、上下文情景、N/D/Q/p、分项成本、总成本、Loss、求解状态、约束残差、使用的 B/C 接口版本。

验收：质量全量计数可核对；p 各分量非负且总和在声明容差内为 1；评分聚合和跨域假设可追溯；检验/外推不混入训练；Q3 输出满足单位、可行范围和预算，并报告未收敛情况。

输出由 A 放入 outputs/a/，接口说明仅 A 修改；B/C 的修改建议写各自交接。
