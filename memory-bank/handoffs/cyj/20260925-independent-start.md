# 独立推进任务启动检查点

已读用户新任务单及公共记忆、数据索引、本人记忆、完整审查协议。确认旧双交互拟合存在 no-quality 指数先估计后固定的两阶段限制；新增 `fit_b7_joint_nonlinear.py` 与解析 Jacobian/模拟恢复测试，先冻结验证设计于 `experiments/cyj/20260925-independent-protocol.md`。

仅基础全样本 joint smoke 已运行：12/12 起点收敛同一近最优盆地、未触参数边界。完整留级、bootstrap、后续取舍尚未运行；不得据初步训练 RMSE 改 v3 模型或科学状态。本检查点用于长任务前备份，后续结果另写交接。下一步 cyj 运行 P0/P2.2，再推进其他独立任务；CHM/ZHH 验收不阻塞这些工作。
