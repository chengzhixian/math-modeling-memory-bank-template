# CYJ → team: V3 Q1-output-only Q2 conditional delivery, 2026-09-25 15:25 CST

## Changes

- Produced and hash-checked the independent Q1 export `outputs/chm/q1_exports/q1_q2_bundle_v1/` in the preceding checkpoint. Its `export_manifest.json` SHA256 is `b0dda7caac30513c0f37e74ccb143064606847fdc35fd7607c999ac2f8647ecb`; it contains all 512 normalized 17-domain Q1 A4 recipes, v1.3 Ridge/QA tables and released Q1 audit metrics. Only `src/chm/export_q1_q2_bundle.py` reads original A4. CHM owner signoff on this incremental Q1 export is pending.
- Added `src/cyj/q2_final_core.py`, `build_q2_final.py`, `ndqp_scenarios_v6.py`, the fixture generator and finalizer, and `scripts/run_cyj_q2_final.ps1`. The Q2 consumer reads only the Q1 derived bundle and published B-side artifacts. It verifies every Q1 bundle file SHA, frozen v1.3 target/column identity, Q1 recipe simplex, p-hull support, Q1 quality coverage and positive conditional Loss factor over the B7 N support.
- Published `outputs/cyj/q2_final/` with 19 hashed files, four new PNG figures plus a figure manifest, and `interfaces/cyj/NDQP_SCENARIO_V6.md` with four fixed request/expected fixtures. The Q2 result manifest SHA256 at delivery is `ada32c3b8df68650f5f83d3d12b1cea1254956c0ceacdaae0573e17c23207beb` (recheck after any regeneration).
- No paper chapter was edited. v4/v5 code and their earlier result hashes were left intact.

## Numeric evidence and reproduction

`& 'scripts/run_cyj_q2_final.ps1'` on the current Windows host performs the separate Q1 export, Q2 build, fixture generation, seven v6 tests, and acceptance finalization. `-SkipQ1Export` runs Q2 alone. Full CYJ suite: 75/75 tests. Two Q2-only runs produced the same `manifest.json` SHA256 before the final validation-table enhancement; the final pipeline passed afterward. `outputs/cyj/q2_final/acceptance.json` reports C1–C8 internal and external tests passed; its scope is conditional engineering delivery, not scientific identification.

At `N=1B, D=100B, Q_B=.5, lambda=1, eta=0, w=1/13`, the Q1-supported Ridge convex-hull optimum is observed Q1 recipe index 136, conditional Loss `2.1928228144395785`, factor `0.8541036111993697`; independent 512-vertex enumeration matches HiGHS LP. Direct-QA and direct-plus-near-QA constraints give LP mixtures with conditional Loss `2.263949598559698` and `2.2749885718994807`. The 13-target minimax mixture gives `2.416838301145758`. At `lambda=0`, all feasible p tie. Q1 `p_ref` has an explicit 512-recipe hull certificate. Q1 quality has 11 unmapped domains left unknown; `Q_A` is never substituted for B7 `Q_B`.

Validation roles in `validation_summary.csv` remain separated: B1 observed training/LOSO/tail and ablation; B2 semi-synthetic and B3 interpolated shape only; B4/B5 within-family/source dominance concordance 131/133 and 113/113; B6's 360 rows nested in B7; B7 24 nested folds and 1350 outer predictions; B8 same-coordinate conflict; B9 metadata and B10 estimated stress only, with B10 max N 835× and D 60× the B7 rectangle endpoints. Q1 held-out 1B Ridge absolute RMSE beats a constant baseline for only 4/13 targets. Three Q1 convex-hull transfers, finite marginals, and in-support Q_B=.5→.6 equal-Loss N/D roots are output. The new 324-row scenario grid is separate from the earlier 540-row v5 grid. Main parameter/residual uncertainty references the frozen B7 calibration; bridge scenario variation is not a confidence interval.

## Outstanding and owners

- **CHM**: inspect/sign off the incremental Q1 derived export's source/normalization/manifest, then consume the exact v6 fixture in Q3 and record owner acceptance. This is separate from already accepted v4 conditional B7/NDQ consumption.
- **CYJ + CHM**: A-to-B joint observations or a defensible calibration design are still absent; `lambda`, `eta` and a common quality coordinate remain unidentified. Keep `ready_for_Q3=false` for a scientific predictor. A Q1 full 13-target interaction coefficient package is also absent; published comparison metrics alone support only model-form diagnostics, so no cross-model regret or nonlinear optimum was invented.
- **Integrator**: review and merge the CYJ branch when interfaces and source statuses are accepted, then update public memory. Personal branch push alone is backup, not main integration.

No original A attachment is opened in `src/cyj/`; the runtime path audit and static source check are in `src/cyj/tests/test_q2_final_v6.py`. The Q1 exporter is explicitly upstream and distinct. The historical hidden-PDF text and discarded Gemini commits were not used.
