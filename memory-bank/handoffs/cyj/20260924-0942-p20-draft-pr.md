# cyj / P20 draft PR 创建交接

状态：已在 GitHub 创建 Draft PR，待集成人验收；Q2 未完成，Q3 预测接口仍 `ready_for_Q3=false`。个人分支 `team/cyj-scaling`，本次起点/此前已核远端 SHA `3e20b3eeca2594b9311adf89442828d468fec026`，基底 `origin/main=a0932fd92b3a46cef8eb0bf563df1e6abc9396ef`（创建 PR 前最近一次 fetch 所见；不推断之后 main 未变）。

## 本次外部变更与证据

- 新建 [PR #3](https://github.com/chengzhixian/math-modeling-memory-bank-template/pull/3)：标题 `cyj：附件 B 审计、经典基线及 B1–B3 诊断`，GitHub 页面核对 `Draft`、head `team/cyj-scaling`、base `main`、23 commits、40 changed files；未请求 reviewer、未标记 ready、未合并。
- 本地 `git diff --name-only origin/main...HEAD` 核对这 40 个文件全部属于 `src/cyj/`、`outputs/cyj/`、`experiments/cyj/`、`problem/cyj/`、`interfaces/cyj/`、`memory-bank/members/cyj.md` 或 `memory-bank/handoffs/cyj/`。本次仅新增本人记忆及本交接，未修改公共记忆或别人目录。
- PR 描述记录测试 19/19 PASS、数据身份核验与 cyj 实验/交接路径、接口草案 v1.6、`ready_for_Q3=false` 和禁止把 B2/B3 当独立验证的边界。PR 创建后尚未由集成人复现或验收。

## 输入版本、命令与未验证项

- 当前 PR 的研究输入/代码见 `experiments/cyj/20260924-classic-baseline.md`、`20260924-b1-precision-sensitivity.md`、`20260924-b2-b3-shape-diagnostic.md`；B2/B3 诊断输入/代码 SHA `3cd66aeb23d9ceccc3958371bf41a212f6699658`，最终交付起点 SHA `3e20b3eeca2594b9311adf89442828d468fec026`。数据清单 `F_MANIFEST.json` SHA256 `3377a36c5f6fcabb79abca5fe5621047a3abd9be6dffd9491bc8c651b19f903f`。
- 已执行 `git status --short --branch`、`git rev-parse HEAD`、`git diff --name-only origin/main...HEAD`；创建 PR 后在 GitHub 页面核对标题、Draft 和两端分支。09:42 的一次 `git -c http.sslBackend=openssl ls-remote origin refs/heads/team/cyj-scaling` 因 github.com:443 暂时不可连接而失败，不把该次失败写作远端已核验。
- 未验证仍包括 B1 Loss 行级来源、B2 与 B1 Loss 可比性、B4/B5 可比性、分组预测区间、Q_A↔Q_score 映射、13 域 Loss↔B1 val_loss anchor/p、Loss–Benchmark 桥接；Q3 正式优化必须等待 cyj validated predictor。全库附件 A LFS 指针阻塞与官方规则待核验状态不因 PR 改变。

## 接口变化与下一步

- 本次没有数学接口或机器结果变更，`interfaces/cyj/CONTRACT.md` 仍为生产者侧 draft v1.6；PR 只提供验收载体，不意味着 `validated` 或 `integrated`。
- cyj：继续 B1 分组不确定性及 Loss 来源、B2/B4/B5 同尺度证据；如无法证实，维持分层/情景表述。chm+cyj：联合冻结 Q mapping、Loss 口径/anchor/p 接法。zhh/集成人：复现检查后决定合并与公共记忆更新，Q4 传播 Loss–Benchmark 桥接误差。
- 本交接提交后执行：`git status` → 明确暂存本人两文件 → `git commit -m "cyj: record draft PR handoff"` → `git push origin team/cyj-scaling` → 比较 `git rev-parse HEAD` 与 `git ls-remote origin refs/heads/team/cyj-scaling`。若 443 故障持续，保留本地提交并如实报告待推送，不能声称远端备份成功。
