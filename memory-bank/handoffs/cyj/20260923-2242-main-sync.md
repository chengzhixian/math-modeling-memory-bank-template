# cyj 交接：同步最新 main 协作规则

- 角色：cyj。
- 任务：暂停当前建模，安全同步最新 main，重读跨分支依赖与 Q/p/Loss 规则，并记录对 cyj 工作的影响。
- 状态：同步与规则复核已完成；建模仍暂停；同步后的成员记忆待随本交接提交和推送。
- 分支：`team/cyj-scaling`。
- 本轮工作起点 SHA：`d0a390ceb4af85eb671799696ac2f041b38100f7`。
- 合并的 main SHA：`a0932fd92b3a46cef8eb0bf563df1e6abc9396ef`。
- 合并提交：`ed1613fda4a8c5a06d80d10cdcc5ee36d4690e85`。

## 输入版本与前置保存

- 同步前个人远端 `team/cyj-scaling` 经 GitHub API 核验为 `d0a390ceb4af85eb671799696ac2f041b38100f7`，与本地合并前 HEAD 一致。
- 同步前唯一未提交修改为 `src/cyj/audit_b_scaling_laws.py` 的审计加固 WIP；已审查并以 `d0a390c` 提交、推送，未丢弃本地工作。
- 该 WIP 只通过 Python AST 语法解析，尚未完成全部修复、测试或重新运行审计，不能视为已验证结果。
- 最新 main 末端由 GitHub API 核验为 `a0932fd92b3a46cef8eb0bf563df1e6abc9396ef`；从共同基线 `e85956fe7cd3ce7e6a6c8b930ae444b1ee93184d` 到该提交为 8 个线性公共文档提交。

## 命令与结果证据

执行或等价执行的关键命令：

```powershell
git status
git add src/cyj/audit_b_scaling_laws.py
git commit -m "wip: harden cyj data audit before main sync"
git push origin team/cyj-scaling
git fetch origin --prune
git switch team/cyj-scaling
git pull --ff-only
git merge --no-edit origin/main
git rev-parse HEAD
git show -s --format="%P" HEAD
```

实际结果：

- WIP 提交：`d0a390ceb4af85eb671799696ac2f041b38100f7`；推送输出显示远端从 `932e22b` 更新到 `d0a390c`，随后 GitHub API 再次核验远端为该 SHA。
- 本机说明中列出的 `H:\Git\cmd\git.exe` 不存在；系统 Git 可用。
- 常规 `git fetch origin --prune` 和定向 fetch 均因 HTTPS 连接超时失败；`git pull --ff-only` 的 HTTPS 连接也再次卡住并由本次会话中止。该失败未改动工作树。
- 为避免猜测，使用 GitHub 官方 API读取远端提交和 blob，并对 8 个 commit 的 parent、blob SHA、tree SHA、commit SHA 逐一复原校验；全部与远端公布值一致后才更新本地 `refs/remotes/origin/main`。
- `git cat-file -t a0932fd...` 返回 `commit`；`git rev-list --count e85956f..origin/main` 返回 `8`。
- `git merge --no-edit origin/main` 成功，使用 `ort` 策略；无冲突，没有执行 reset、ours/theirs、强推或整文件覆盖。
- 合并提交父节点为 `d0a390ceb4af85eb671799696ac2f041b38100f7` 与 `a0932fd92b3a46cef8eb0bf563df1e6abc9396ef`。
- 合并只从 main 引入 8 个公共规则文件；cyj 自有文件未在合并中被覆盖。

## 重读范围

合并后已完整重读：

- `AGENTS.md`
- `TEAM_WORKFLOW.md`
- `TEAM_COLLABORATION_DEPENDENCIES.md`
- `memory-bank/activeContext.md`
- `memory-bank/progress.md`
- `memory-bank/systemPatterns.md`
- `interfaces/README.md`
- `TASK_PLAN.md`
- `memory-bank/members/cyj.md`
- `memory-bank/handoffs/cyj/20260923-1827-p01-b-audit.md`
- `interfaces/cyj/CONTRACT.md`

## 当前任务状态与结果证据

- 已验证：附件 B 结构/身份审计有既存运行记录，19 个 CSV、10,484 行，原运行结果为 97 pass、3 warning、0 fail。
- 尚未验证：审计脚本加固 WIP 没有完成全部问题修复，也没有重新运行；B1 拟合、B2–B5 验证、外推与不确定性估计均未开始。
- 当前没有可供 Q3 正式优化调用的 validated predictor；`interfaces/cyj/CONTRACT.md` v1.1 仍是 draft、audit-only。
- 早期审计 JSON 的工作区 CRLF SHA256 为 `0fa89425b2c37eb00dac1db8f8889199e895f5d8245d3e174ca30e18dded5e36`，已提交 LF 内容 SHA256 为 `fe07d4a142f424950a63ec7689d35cb9df05d74a15a56a2316a0477bc90e4b6a`；后续必须明确字节版本和哈希口径。

## 新增合作要求与依赖

1. `Q_A` 不等于 B6–B8 的 `Q_score`。chm+cyj 必须共同冻结主 mapping、敏感性 mapping、有效范围和 mapping 不确定性。
2. Q1 的 13 个 domain Loss 不等于 B1 `val_loss`。chm+cyj 必须共同冻结 Q2 Loss 定义、主 anchor、至少 3 个敏感性 target、p 的函数形式、中心化参考配方、尺度传递参数和有效范围；不得默认 `pile_cc == B1 val_loss`。
3. Q3 正式优化必须等待 cyj 的 validated predictor。chm 在此之前只能搭建求解器框架，不得发布正式最优配置。
4. C7 的 2048 / 8192 / 131072 Token 由 zhh 提供并作为外生敏感性情景，不作为连续内点寻优变量；正式使用前仍需 zhh 更新合同并通过集成验收。
5. Loss 不等于 Benchmark。Q4 必须传播 cyj/chm 的上游参数不确定性与 zhh 的 Loss–Benchmark 桥接误差，外推超出覆盖范围时不得作为主结论。
6. 跨成员接口消费必须记录来源分支或 main、精确 commit SHA、接口版本、文件 SHA、单位、有效范围及 draft/validated/integrated 状态。

## 接口变化

- 本轮没有修改 `interfaces/cyj/CONTRACT.md`，也没有新增预测函数、参数文件或 validated 接口。
- 公共规则改变了未来验收门槛：cyj 的预测接口必须明确 Loss 定义以及 N/D/Q/p 的尺度、单位、有效范围、验证误差、不确定性、真实测试案例和可复现调用方式。
- 与 chm 的 Q mapping、Loss anchor 和 p 接法仍待联合冻结；这些不是当前已确定事实。

## 未验证项、下一步与负责人

1. cyj：先完成并重新验证 `d0a390c` 的审计加固；再建立 B1 N-D 基线及 B2–B5 预注册验证边界。
2. cyj + chm：联合冻结 Q mapping、Loss 定义、anchor、敏感性 target 与 p 接法；双方记录精确输入 SHA 和接口版本。
3. cyj：产出 validated predictor 之前，不把任何占位或 draft 结果交给 Q3 作为正式输入。
4. zhh：更新 C7 与 Loss–Benchmark 合同并完成集成验收；Q4 传播桥接误差。
5. 集成人：验收本次个人分支同步记录后再汇总公共记忆；本轮不由 cyj 修改公共文件，也不把上述待联合决定事项写成既成事实。
