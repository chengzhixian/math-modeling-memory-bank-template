# CYJ Q2 v8 conditional acceptance handoff — 2026-09-25

The user-provided competition-role checklist is implemented as `cyj.ndqp.scenario.v8`, without changing published v7 files. The formal law pins B1's five N-D coefficients and fits B7's three quality coefficients; Q1 v2 mapped `Q_A(p)` enters Loss through an explicit uncalibrated monotone proxy, while the frozen Q1 relative p effect enters as a separate factor. The exact API, units, support and assumptions are in `interfaces/cyj/NDQP_SCENARIO_V8.md`. The B7 joint eight-parameter fit remains a comparator.

Reproduction on this host: set `PYTHONPATH=.task_deps;src/cyj` and use Python 3.12 with NumPy 2.3.5/SciPy 1.18.1. Run `python -B src/cyj/audit_q2_v8.py`, then `python -B src/cyj/finalize_q2_v8.py`. The latter checks two deterministic builds, pinned B1 provenance, 24 nested held-level folds, the explicit Q1 quality calculation and independent Loss formula, all 136 feasible domain pairs, local/finite quality-scale effects, B4/B5 support-stratified directions, B9/B10 stress roles, migration, Q3 fixed-p smoke, 18/18 fixtures, 95/95 CYJ tests and v7 file immutability. The audit observed 0 original-A Python file opens and 45 derived-Q1 opens; native-library file access is outside that audit's coverage. Manifest and acceptance are in `outputs/cyj/q2_v8/`.

B3's eight interpolated trajectories yield mean normalized RMSE 0.0018918 against B1; B2 is semi-synthetic stress. Neither is independent real external validation. B4/B5 report only within-family directional comparisons, with 12/12 and 7/7 agreements inside the joint B1/B7 support; absolute cross-source RMSE is unclaimed. All 128 B10 rows exceed B1 N support and B10 remains estimated stress. The quality proxy and p interaction may overlap in content; no paired A/B experiment identifies their independent contributions. `q2_competition_requirements_complete=true` is therefore a **conditional checklist/engineering acceptance**, not empirical calibration or a claim that full Q3/paper submission is done.

The Q3 v8 fixed-p cost smoke is feasible and reuses the Q1-derived proxy, but final joint Q3 optimization and CHM owner consumption remain CHM's work. Main integration and public-memory update remain the single integrator's work. Remote SHA confirmation and release record must be added after pushing this checkpoint. No raw A was read and no paper layout was started.

Release addendum: accepted subject commit `615c078517379284f6b0504c568790a7975c6eda` was pushed and independently matched by `ls-remote`. `outputs/cyj/q2_v8_release_verification.json` records the checked subject manifest and acceptance hashes; this addendum and release record require a second push and SHA check. The post-release finalizer must not be rerun because it regenerates the pre-release state.
