# Q2 competition-role v8 WIP handoff, 2026-09-25

The new user task requires B1 parameters for the classic N-D backbone, B7 quality-only extension, a Q1-Q_A-derived quality proxy, quantitative B3 trajectory checking and explicit domain pairs. It explicitly prohibits altering v7 historical outputs and starting final Q3 large-scale optimization or paper layout. CYJ has therefore added `cyj.ndqp.scenario.v8` and `outputs/cyj/q2_v8/`, leaving the published v7 tree intact.

Current generated evidence: `b7_quality_extension.json` fixes the five parameters from B1 SHA256 `9b0e381f...`; its three B7 quality coefficients were selected after nested held-level CV, with train RMSE 0.0485401 and N/D/Q outer mean RMSE 0.0487383/0.0487507/0.0486387. `b7_backbone_comparison.csv` retains the old eight-parameter model and distinguishes its fixed-family held-level CV from separately published nested family selection. B7 remains semi-synthetic and some fit rows lie outside B1 support, so the formal predictor uses the B1/B7 common support only. `b2_b3_model_validation.csv` computes true model-vs-observed normalized metrics; B3 is interpolated, not independent validation. `domain_pair_substitution.csv` and `domain_pair_interaction.csv` cover 136 domain pairs at an interior A4 mean recipe, including LP-based forward/reverse feasible epsilons. `scale_order_validation.csv` stratifies B4/B5 directional agreement by support; common absolute Loss coordinates are unverified. B10 stays estimated stress only.

The v8 baseline quality proxy has six direct+near mapped Q1 domains (or three direct in sensitivity), explicitly excludes eleven unknown Q_A values, and reports coverage/unmapped mass. It applies an unfitted monotone min-max anchor to B7's `[0.1,1]`, then multiplies the Q1 p relative-effect factor. Its potential double counting is not identified. `interfaces/cyj/NDQP_SCENARIO_V8.md` documents the conditional interpretation and Q3 method. The first 18 frozen request/expected pairs replayed successfully. No A/B empirical bridge calibration or CHM Q3 owner acceptance is claimed.

Reproduction so far: set `PYTHONPATH=.task_deps;src/cyj`, then run `python -B src/cyj/fit_b7_quality_extension_from_b1.py`, `python -B src/cyj/build_q2_v8.py`, `python -B src/cyj/build_v8_fixtures.py`, `python -B src/cyj/verify_v8_fixtures.py` with Python 3.12, NumPy 2.3 and SciPy 1.18. Do not set `q2_competition_requirements_complete` yet: independent finalization, full CYJ/v7 regression, raw-A audit, finite-difference checks, Q3 fixed-p smoke and remote publication verification are pending. Next owner: cyj. Main integration and real CHM consumption remain with their respective owners.
