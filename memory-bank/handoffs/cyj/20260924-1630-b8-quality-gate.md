# cyj 交接：B8 质量数据兼容性与消费限制

日期：2026-09-24。分支 `team/cyj-scaling`；输入 HEAD `cebd51bd0116ef3194728cfbeed9569239e085db`，main `7d8081fbf50cd380904505759c116580356f102d`，CHM `7c14a0c894072048d09f04bd03653be1301f7257`，zhh `d47cd2dc921333caecfcb95f09eb5a2f2714d0db`。本轮远端 fetch 一度因低速失败，不能声称这些是实时最新远端版本。

## 已完成及证据

- 新增纯标准库诊断脚本及 3 项单元测试；原始 CSV 的清单 SHA/bytes 校验通过，29 项总测试通过；两次最终诊断哈希一致。
- `outputs/cyj/diagnostics/b_quality_audit.json` SHA256 `1d9576dc63bdc078b72f0dd530e8b568bed68ca3ad905c49543ee0d3c734684d`。代码内容哈希/输入版本/数据身份均内嵌；复现命令和完整表见 `experiments/cyj/20260924-b8-quality-audit.md`。
- B6 360 行均重复于 B7；B7/B8 224 同坐标 Loss 全异。B8 calibrated 90 组 Q 端点 Loss 均上升，B6/B7 各 45 组均下降；存在局部逆序/持平。B8 362 行恰为最小 Loss=0.5，截断原因未知。
- 原始数据、公共记忆和其他成员文件未改动；无中间 CSV/缓存。此前 B1 消融、CHM 采用、Q3 API 交付保持有效但仍为 draft。

## 接口变化与分工

合同 v1.9；新增 `cyj.b_quality_audit.v1` 的 `usage_policy`，预测器 `cyj.q3.v1` 不变。B8 calibrated 也须隔离于 B6/B7 共同拟合，不能按 calibrated 标签直接准入。旧 audit 的 fit_eligible_count 只是标签统计，不作为本轮科学准入。

- chm：可调用 B1 diagnostic、显式 p/target/lambda/eta scenario、成本/约束；formal/Q 性能仍拒绝，不得以本轮交付宣称正式 Q3 完成。请验收 CHM 样例及换行哈希兼容问题。
- zhh：C7 外生情景保持原定义；不能将重复 B6/B7 或外推 B8 当独立验证，Q4 继续单独传播 Loss–Benchmark 桥接误差。
- cyj 下一步：去重 B7 原生 Q 候选模型及组级验证，同时核查 B8 生成说明/Q 方向/Loss 口径；没有证据前不改原始 Q 或 Loss。
- chm+cyj：共同尺度、主 anchor、lambda 仍待冻结；Q_A≠Q_score，13 域 Loss≠B1 val_loss。
- 集成人：验收后汇总公共记忆；参考论文索引只用于证据展示组织，本轮未读原 PDF、未借用其模型或结论。

## 未验证与备份状态

尚无完整 validated N-D-Q-p predictor；B1↔B质量 Loss 桥接、A↔B Q 标定、B8 异常成因未明。没有将异常视作已确认数据错误，没有新质量拟合。全部附件 LFS 完整性未在本轮复跑。

推送前命令：`git status --short --branch`、`git fetch origin --prune`、`git diff --check`。明确暂存本人本轮文件后提交并 `git push origin team/cyj-scaling`，最后 `git rev-parse HEAD` 与 `git ls-remote origin refs/heads/team/cyj-scaling` 比对。此前 fetch/push/ls-remote 遇到连接重置或低速超时；本记录不把本地提交当作已成功备份。最终执行结果以收尾工具输出与回复为准；若仍失败保留提交，下次先补推。
