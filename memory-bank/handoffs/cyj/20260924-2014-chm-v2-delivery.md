# cyj → chm：可调用 cyj.chm.v2

2026-09-24 20:14 北京时间；分支 `team/cyj-scaling`，工作起点 `ad1d312c15bcf79c23d715dd199eec7c2e65a0af`。本轮 fetch 因 connection reset 失败，固定使用已核验的 main `af4857045e5df62afc9815bce4b98dbbb8867253` 和 chm `a5525935b37f873235d2f650e4810a787b9a8788`，不把远端浮动版本当输入。仅改 cyj 归属文件。

交付说明 `interfaces/cyj/CHM_API_V2.md`；实现 `src/cyj/chm_adapter_v2.py`、生成器 `src/cyj/build_chm_release_v2.py`、测试 `src/cyj/tests/test_chm_adapter_v2.py`。`CHMAdapter(mode="diagnostic").value_grad(N_B,D_B,Q)` 兼容 chm LossModel 协议；evaluate/批量 JSON 返回 B7 NDQ、梯度、同编号条件样本、题面成本、预算残差和独立 Q1 v1.2 的 13-target p 向量。manifest 提供支持域/参考配比及 policy。无 eta/跨 Loss 加法，p_policy=sensitivity_only、unique_p_claim_allowed=false。旧 v1 不覆盖。

## 输入与机器交付

- chm `chm.q1.v1.2` manifest Git 原始字节 SHA256 `5885317d072739b02cdbb434fc730dde07510eb284dc857e35863adc877e914d`；由生产者原生读取器复核五文件 LF 规范化哈希/行数，仅读固定 Git 对象，未重复处理 A 原始数据。
- B7 fit SHA256 `e676bfb06da81c02ad968591aa9ae09da5c7cd6b56def49d59a82c371465b025`，原拟合输入/代码 `6c17cb4e387e1ac047f8fe0e9ecb6fe42fc5ef3b`。沿用原生半合成 Loss 坐标、参数及区间限制。
- `outputs/cyj/interfaces/chm_v2_manifest.json` SHA256 `846d72b583ba5f065a6ea2f6915e49f2ce77962848dd496f23054fc3cd2a248e`，包含七个固定上游 blob SHA 和逐文件加载校验，以及 CHM smoke test 源文件哈希。
- `chm_v2_request.json` SHA256 `e97788d1de2df48199b3910abee20159d204ce039c6a79b4d9c8de6a70348b71`；`chm_v2_expected.json` SHA256 `80f38e44a8aa087857cbeaa5253b32b48ed57831ba861127504dfd91535fd9c3`。manifest 内列代码/样例 LF 哈希，精确实现提交由本交接所在 Git 提交定位。

## 命令与结果

本机 Python 为 `C:\Users\muyehuangyi\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`（3.12.14；NumPy 2.3.5）。在下列命令用该解释器代替 python：

```powershell
git fetch origin team/cyj-scaling integration/chm-q1-clean-20260923
python -B src/cyj/build_chm_release_v2.py
python -B src/cyj/chm_adapter_v2.py --describe
python -B src/cyj/chm_adapter_v2.py --request outputs/cyj/interfaces/chm_v2_request.json
python -B -m unittest discover -s src/cyj/tests -p 'test_*.py' -q
```

44/44 测试通过；已补固定七个上游 Git blob SHA 核验、旧 eta 参数拒绝以及显式 Loss/support/uncertainty 输出。测试包括原生 Q1 v1.2 参考点零向量与重配系数差、NDQ 三维梯度差分、错误类型/旧 B1 边界拒绝、p 不改变 B7 Loss、低预算无解预检、仓库外目录 CLI 及整批原子失败。实际执行 chm readiness v2，Q/p policy 通过结构检查，正式门槛保留三个阻塞：ready=false、quality performance/joint_NDQ 非 validated。

样例 Loss=3.492870995028143 与 3.4204719377890855；成本约 4.48672e18 与 9.146799411773743e18 FLOPs，均满足 1e19 预算。数值只用于接口验收，不是预算最优结果。

## chm 接入及未验证项

**chm 下一步：**固定本交付 SHA，读取 model.bounds：N=[.07,11.97]、D=[10,600]、Q=[.1,1]；将 `q3_generic_solver.py` 中 solve_generic 和 active_set 的 B1 硬编码边界同时改成模型边界，Q≥Q0。不能只替换模型而保留 D_min=.134。当前环境无 SciPy，本轮未运行 chm SLSQP、未修改其目录；其联调与正式发布由 chm 验收。p 支持只检查 simplex，训练凸包需 chm 检查。

**科学状态：**diagnostic_only，ready_for_Q3=false。新版已解决旧 eta 依赖的工程迁移；B7 半合成来源/选模后的独立验证、总预测区间及团队验收仍待 cyj 完成。A↔B Q 映射不是 B-native NDQ 的必需门槛，p 可正式声明 sensitivity_only，不人为补造桥接。集成人验收后汇总公共记忆。最后额度检查主窗口剩余 10%，优先提交推送本交付；远端成功以收尾 ls-remote 为准。
