# CYJ v5 conditional NDQP continuation checkpoint

- Scope: CYJ Q2 conditional four-variable model and Q3 theory; start from local `8bf98a7` (remote GitHub connector backup `5273160`, identical file tree, different history). Local Git HTTPS remained unavailable. Do not force push; fetch and merge the connector backup when Git connectivity returns.
- Producer and source: CHM Q1 v1.3 pinned to `cdda1ad62c5c7eb72b413c4228caeff87d2bad30`, manifest SHA256 `925cf317c861e5f65d7f2ebdaebc693bc7f4b60d17d486488e87858bce4b5d80`. Frozen B7 joint artifact unchanged. No raw A reprocessing, no hidden PDF text or discarded historical models.
- Changes: v5 standard-library JSON batch CLI, explicit lambda/eta/weight inputs, global B7-N factor positivity for each exact p, finite difference and support tests, 3 fixed CHM fixtures, 17 raw B file hashes and per-requirement fields/units/roles, 540-cell mapping-confidence scenario grid, weight robustness summary, 4 manifest-verified figures, updated Q2/Q3 paper with two figures and CHM certificate conditions.
- Reproduction: `python -B src/cyj/build_ndqp_v5_fixtures.py`; `python -B src/cyj/build_q2_ndqp_evidence.py`; `python -B src/cyj/plot_q2_ndqp_scenarios.py`; `python -B src/cyj/run_full_audit.py`. SciPy 1.18.1/Matplotlib 3.11.2 were installed only in ignored `.venv/cyj-deps`; set `PYTHONPATH` to it for plot/full audit. `outputs/cyj/q2_joint_scenarios/manifest.json` and `figures/cyj/q2_joint_scenarios/manifest.json` carry hashes.
- Actual checks: full audit 19/19 checks PASS_WITH_LIMITATIONS; 68/68 CYJ unit tests passed; 540 grid points, zero scenario failures; four figure hashes verified; XeLaTeX compiled 11-page team draft without overfull boxes. Visually inspected CYJ Q2/Q3 pages 5--10. `run_full_audit.py` output files record individual checks.
- Review gate: B1 same-source reconstruction, B7 semi-synthetic conditional fit, B2/B3 shape, B4/B5 source-coordinate rejection, B6/B7 overlap, B8 conflict, B9/B10 estimated stress. The from-zero field review still finds no paired same-Loss N/D/Q/p observations. Lambda/eta/Q_A-to-Q_B remain unidentified. The 1M/60M paired CHM rank output is available, but direct signed/residual paired effects and a producer-certified p training convex hull are not supplied to this interface. Those claims remain unverified. `ready_for_Q3=false` and formal four-variable predictive calibration remain blocked by evidence, not by software tests.
- Scenario interpretation: github-to-arxiv 1% transfer lowers conditional Loss for all three prespecified target weights with lambda>0 on this grid. Freelaw or pile_cc transfer direction depends on target weights. These are model-conditional rankings, not causal or held-out B validations. CHM's B7-only floating-point objective certificate does not apply to nonzero lambda without new proof.
- Next: CYJ can complete a fresh 18-stage written red-team and producer-side paired-effect validation if CHM publishes a machine contract. Integrator handles public memory. Remote backup of this continuation must be verified separately after commit; do not claim local commit SHA equals remote connector SHA.
