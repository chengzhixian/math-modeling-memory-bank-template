# Q1 前两小问证据补链交接

成员：chm；日期：2026-09-25；分支：`integration/chm-q1-clean-20260923`；前置 SHA：`a43fc88`。

## 已改与复现

- `src/chm/q1_quality_robustness_final.py`：A1 70/30 分层固定留出，训练集 100 次分层方向重抽样；输出22字段规则表、方向/排名分布和既有八种方法规则名次面板。A1 SHA与当前质量 manifest 一致。主 `Q_A` 和 v1.3 未更改。
- `src/chm/q1_conflict_resolution.py`：沿用55条冲突边，冻结 A1 Q中位数 0.02725594 和 D 第75百分位 2.04229706；输出样本分级、缺失原因、去重 A2/A3 固定阈值复核、27条分层原文索引、原文SHA和22个定向值，不输出原文。
- `src/chm/q1_text_case_review.py`：仅在本机按选中 `(domain,id)`、源行和SHA回读最多2000字符，不写文件。`test_q1_conflict_resolution.py` 覆盖非惩罚动作、双缺失和输入字段拒绝。
- 解释与正反案例见 `experiments/chm/20260925-q1-quality-conflict-evidence.md`，论文 LaTeX/草稿已同步。

## 实跑结果

- 100次分层训练重抽的14字段符号与七域排名均不变；LOO仍有5字段翻号。八种方法规则下 book/arxiv 排名1--2，c4/github 5--7；方法包络不是95%CI。book仅171条。
- A1决策：保留20384、保留并复核5231、低优先级18038、低优先级并复核7577。七域D缺失0；去重A2/A3复核触发率69.04%/47.08%，不是准确率。
- 27条固定抽样案例中，book高Q高D版权开头极长文本、c4低Q低D事务公告提示分数/分歧不能当真实标签。无人工盲评标签，语义判断尚需参赛者独立复核。
- `python -m unittest discover -s src/chm -p 'test_q1*.py'`：22项通过。最新版 `src/chm/build_paper.ps1 -OutputName chm-q1-quality-conflict-20260925`：24页PDF成功，引用/字形/溢出门禁通过；构建清单及PDF留在 `paper/latex/output/`。

## 尚未冻结与下一步

- 第三小问 A4--A15 配比本轮未动；用户要求修改前先讨论，需确定是否增加交互候选、如何定义“组合影响”及接受1B绝对误差失败的论证方案。
- 案例不是独立人工质量真值；方向留出独立于本轮符号拟合，但原A1预处理/主规则曾使用全A1，不能称完全外部验证。最终冲突边跨范围联合FDR无5%保证。
- 本轮属于前两小问证据补链，不是 Q1 总体最终冻结。新不可变机器接口 v1.4、配比闭环及正式整篇视觉校验待后续。
