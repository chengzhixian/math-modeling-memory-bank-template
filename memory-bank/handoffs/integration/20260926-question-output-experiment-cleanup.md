# main 问题目录与核心实验清理审查

## A. 审查范围与 Git 冻结

基线为远端 `main@0d1b511c014bc1558b1bfc226b72ba1b534a01da`；对照远端 CHM `c052b6918c3f77a2285622521d8abb1b429513be` 和 CYJ 最新 `74e678e319b58e2aab230a7b7233fa53f3053fa5`，其中 Q2 v8 数值固定于 CYJ `fd2dbb3b2002983430329cdb2ec6a275c2eed4f6`。原始本机工作树的暂存区没有用作来源；在隔离工作树 `E:\q123` 集成。范围是 `outputs/chm` 能否删除和 Q1–Q3 实验记录是否应进入 main；没有重拟合所有模型或审核 Q4。

## B. 内容清点、数据与变量追溯

清理前 `outputs/chm` 有 14 个跟踪文件：九个属于 Q1 v2 冻结接口（17 维配方、13 目标系数、A 侧质量/域映射、验证和折级记录）；其中四个与既有 `outputs/Q1` 文件字节相同，五个为 Q1 目录缺失的接口输入。剩余五个为两份根目录重复文件、旧 Ridge 对照 CSV、已在 Q1 存在的凸包界和签核副本。九个接口文件现都在 `outputs/Q1`，逐个仍符合 `interfaces/chm/q1_interface_v2.json` 原 SHA256；原清单文字及整体 SHA `c621f7e405106e42720f918f490235b5e8becefb0f7c8cb997736e96337117a9` 不变。main 读取器只将旧发布路径映射到问题目录，防止删除旧目录后静默改用不同模型。

| 问题 | 主要数据角色 | 入 main 的核心实验 | 不能升级的结论 |
|---|---|---|---|
| Q1 | A1–A3 质量/冲突；A4/A5 训练；A6–A11 曾参与形式选择；A12–A15 estimated | `experiments/Q1/CORE_VALIDATION.md`，九个冻结接口输入和数值签核 | A/B 质量桥、跨规模连续律、完全盲测及真实 10B/70B 最优 |
| Q2 | B1 同源真实日志；B7 半合成质量；B6 重叠；B10 estimated | `experiments/Q2/` 的 B1 基线、v8 质量扩展和完整审查 | Q/p 到 B 总体 Loss 的实证标定或因果效应 |
| Q3 | Q1/Q2 冻结条件接口、题面成本、C7 外生上下文 | `experiments/Q3/` 的完整审查与真实 N/D 部分外测 | 87 条已观测配方之外的全局联合最优，或真实训练无条件四变量最优 |

核心变量 `N,D,Q_A,Q_B,p,Loss,L_ctx,cost` 的定义、单位、支持域与识别等级见 `experiments/Q2/V8_REVIEW.md`、`experiments/Q3/REVIEW.md` 和先前集成审查。新目录没有从历史隐藏 PDF、作废 Gemini 工作或旧 v7 主数值恢复内容。旧 Ridge CSV 不进入新的主结果；其对照数值保留在冻结验证表和来源提交。

## C. 全协议发现与处理

**MAJOR（结构与可复现性）**：上次 main 同时存在按问题结果目录和 `outputs/chm`，但正式 Q1 v2 清单仍直接读取后者；直接删除会破坏接口。处理：先把五个缺失输入迁入 Q1，四个原有同字节文件复用，更新 Q1 读取器做严格只读别名及原哈希校验，再删旧目录；不得改写来源分支清单。main 中其他旧 CHM 诊断脚本仍可能指向来源分支路径，完整生产复跑须在固定来源提交执行，main 只保证精简发布接口与结果可核验。

**MINOR（哈希口径）**：Q1 旧签核 `numerical_signoff.json` 的 bounds 字节哈希 `b27939f9...` 对应 Windows CRLF 工作区版本；main/Git 的同内容 LF 文件 SHA256 为 `969f810c...`。逐行换行转换后两者相符，数值未改变。更新合同中的 main 文件哈希，保留原签核原文并注明差异。

**MAJOR（实验缺口）**：`experiments/experiment-log.md` 原先只有空模板，关键实验只在成员分支或结果摘要中。处理：按题选取 Q1 冻结交接的实验汇总、CYJ B1 基线与 v8 完整审查，并单列 v8 B7 固定主干实验；将 CHM Q3 两份已在 `outputs/Q3` 的审查原文移到 `experiments/Q3`，避免重复。原始大表、旧版比较和缓存留成员分支，main 通过精确 SHA 和命令追溯。

**NOTE（科学 Gate）**：Q1 质量是工程代理，A6–A11 非最终盲测；Q2 B7 半合成，B6 重叠，B10 estimated；Q3 外部 42 模型仅核 N/D 子结构。文献提供候选方法，不证明本题 A/B 桥；公式与代码由冻结接口、数值表、测试和独立复核共同核验。基线/消融/敏感性与支持域分别写入三个实验目录；数值稳定不等于跨源参数可识别。忽略现有模型、只从可见题面和表头推导，也无法得到经验 A/B 桥或真实 Q3 联合最优，故不提升此前结论等级。

## D. 验证、未解决问题和负责人

本轮以 Q1 v2 原清单九文件哈希、512×17 配方/13 目标加载、Q2/Q3 精简清单、文档链接和 Git diff 作为集成门禁；实际命令和远端 SHA 以本轮结束记录为准。正文实验记录包含复现入口与独立验证限制。Q2/Q3 仍是 **VALIDATED FOR STATED SCOPE** 的条件答案。完整 A/B 成对标定、Q4、正式 PDF 构建不在本轮范围，由对应问题负责人及后续集成人处理。普通成员继续在个人分支保留全量实验；集成人只把验收后的精简实验纳入 main。
