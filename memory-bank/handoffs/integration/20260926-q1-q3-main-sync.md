# Q1–Q3 远端成果选择性集成交接与审查

## A. 范围与来源冻结

集成基线为远端 `main@f9693bbf4c205aa46d3719f6f8a1d6f26561d05f`；来源为远端 `integration/chm-q1-clean-20260923@c052b6918c3f77a2285622521d8abb1b429513be` 与 `team/cyj-scaling@046abaeede95868827fd001dcfee62eb58614eaa`。Q2 v8 数值生产者固定于 CYJ `fd2dbb3b2002983430329cdb2ec6a275c2eed4f6`，CHM Q3 使用同一生产者。审查对象为 Q1–Q3 的结论、论文段落、精简结果及跨题接口；未重新生成全部原始结果，也未审核 Q4 或全稿提交规则。各问原发布包的代码、完整表及哈希仍在来源提交，精简副本由各目录 `curated_manifest.json` 校验。原工作树中的暂存内容及本地旧分支未作为来源。

可信来源：可见题面 DOCX、`problem/readable/DATA_DESCRIPTION_VISIBLE.md`、已校验原始附件、Q1 v2 接口、CYJ v8 冻结包、CHM Q3 v8 发布包及其外部验证记录。禁止来源：旧 PDF 隐藏边字、作废的 Gemini 历史提交、旧 Ridge/v7 作为当前主数值、无来源聊天结论。历史文件不迁入 main。

## B. 题面—数据—变量追溯

| 要求 | 输入与字段 | 角色和单位 | 可支持的结论 |
|---|---|---|---|
| Q1 质量与冲突 | A1–A3 的 22 信号及域标签 | 原始/扩展样本；`Q_A` 是无量纲评分代理 | A 内域质量、指标分歧、去重稳定性；非人工真值 |
| Q1 配比与 Loss | A4/A5 512×17 配方和 13 域 Loss；A6–A11 实测组；A12–A15 estimated | `p` 为比例，Loss 为各域数值；后两组仅压力测试 | A 侧 1M 相对效应及有条件决策；不能识别连续 N–D 律 |
| Q2 规模律与质量 | B1 `N_params_B,D_tokens_B,val_loss`；B7 `Q_score` | N 为十亿参数、D 为十亿 token；B1 实测，B7 半合成 | B1 同源 N/D 主干、B7 条件质量增量；不能把 B7 CV 当真实外测 |
| Q2/Q3 质量与配比桥 | Q1 `Q_A(p),r_w(p)` 与 B7 `Q_score` | `Q_A→Q_B` 及 `exp(r_w)` 为假设/情景 | 具名跨附件条件 Loss；桥参数不可识别 |
| Q3 预算与上下文 | 题面三项成本、C7 `max_position_embeddings`、Q2 v8 预测器 | FLOPs 与 token；成本系数、Q0、L 均为情景 | 三成本族、三预算及额外预算的条件配置和转移 |

核心式 `L=[E+AN^{-α}+BD^{-β}+(1-q)G(N,D)]exp(r_w(p))` 中，B1 识别同源 N/D 经验主干，B7 提供半合成质量响应，`q(p)` 与 `r_w` 来自 A 侧并经人为跨源桥接；各系数、单位、支持范围见 `outputs/Q2/model_definition.json`。Q3 在共同 N/D 支持域和 87 条可行 A4 已观测配方中优化。不得把 A/B 同名 Loss、质量或域效应视作天然同一变量。

## C. Gate 发现、处理与等级

**BLOCKER（无条件科学结论）**：没有同条件 A/B 配对训练记录，跨源质量映射、配比到 B 总体 Loss 的系数及其随规模变化不可识别。处理：所有 Q2/Q3 主结论、论文与目录入口均明示条件性；无条件四变量最优仍为 NOT READY。若要解除，需要可比 Loss 坐标下的成对干预实验并重拟合、重跑三问。

**MAJOR（验证独立性）**：A6–A11 曾参与 Q1 模型形式选择，B7 半合成同源折验证，B6/B7 坐标重叠，B10 和 A12–A15 为估算数据。处理：论文将 A6–A11 记作真实比较而非完全未触碰盲测，Q2 的 B7 指标记作半合成同源验证，估算数据只用于压力测试。Q3 外部 42 个真实模型只核 N/D 子结构，不能检验 Q/p 联合最优。

**MAJOR（配方支持与成本）**：Q3 数值联立仅枚举 87 条满足质量政策的已观测 A4 配方；不能宣称整个连续凸包全局最优。Q0、质量成本函数、C7 上下文长度、预算均为指定情景。处理：固定政策、离散联立和原生自由 Q 的表分别列出，声明 30/36、33/36、33/36 可行格及 82 个数值转移；保存九个桥接敏感性情景摘要。修改 Q0/桥映射或扩展支持域须重跑 Q3 全表和转移。

**MINOR（工程）**：main 原有 `outputs/chm` 与 `src/chm` 是既有复现路径，部分旧论文图、个人文件与新成果不一致。处理：新发布成果和论文分别放入按问题命名的目录；删除已弃用的论文图，不批量迁移仍被旧代码引用的历史复现文件。XeLaTeX 在本机不可用，当前组合稿未完成 PDF 编译和版式验收；有 TeX 环境后须按 `paper/latex/README.md` 构建。

文献只用于说明逐域建模和成本形式的适用背景；数值以附件和冻结结果为准。公式↔代码核对依赖 Q1 接口、CYJ v8 fixture 与 CHM Q3 发布校验；检查单位、有限值、预算和质量约束。Q1 Ridge/交互及质量规则消融、Q2 B1/B7 基线与嵌套折、Q3 固定/联立/原生 Q 以及桥接和成本敏感性均已进入精简答案及来源包。独立于发布叙述，从题面和字段重推只能得到 A 内相对配比、B 内 N/D 与半合成 Q 的经验关系；不能由数据推出跨附件因果桥。这是本次 red-team 的核心边界。

## D. 复核与下一步

在固定来源树重放 CYJ `test_ndqp_v8.py` 11 项、v8 fixture 18 项及 CHM Q3 单测 9 项，均通过；CHM Q3 发布清单校验通过。精简副本与上游哈希比对：Q1 6 个关键原样文件、Q2 16 个冻结包文件、Q3 8 个发布表/JSON 一致；其余答案、审查文字是路径调整或集成说明。集成人仍须执行精简清单自身校验、论文静态引用检查、Git diff 检查、远端 main fast-forward 推送及 SHA 回读，结果以实际记录为准。

最终判断：**Q1 已验证其 A 侧范围；Q2 和 Q3 为 VALIDATED FOR STATED SCOPE 的条件答案**。当前集成可作为 main 中 Q1–Q3 的简洁共同版本；Q4、跨源实证校准和正式论文 PDF 不属于本次完成范围。下一步由各问题负责人维护原始生产分支，集成人维护公共索引并在新数据或接口变化后重新验收。
