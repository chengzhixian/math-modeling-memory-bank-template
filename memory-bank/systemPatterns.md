[PDF-HIDDEN-TEXT-BLOCK]

历史版本《数据说明.pdf》（SHA256 `f5c851bbe4b3d8c9079609c37f2c3b0835244067711d6d761c66adeaa8357835`）第 2—13 页曾含近白色 5 pt 隐藏页边文字。用户明确要求：这些历史隐藏内容及其衍生的方法、参数、阈值、数值和结论绝对不能作为建模依据。

当前仓库 `problem/F/数据说明.pdf` 已在提交 `55743ca` 替换为清理版：13 页、13207231 bytes、SHA256 `daa8479decf578ec5f0b165683ac5715fed445ba8c148f386e7b071f5c27dfdc`；本地复核结果为可提取文本字符数 0、旧隐藏文字特征 0、近白小字号字符 0。当前 PDF 主要用于页面视觉核对；机器可读正文继续使用 `problem/readable/DATA_DESCRIPTION_VISIBLE.md`。

Gemini 历史提交 `fd55147`、`80c7ea5`、`6086964` 的工作因无法可靠排除隐藏文字影响而作废，提交 `58f4f0a` 已明确丢弃其文件内容。不得从 Git 历史恢复、cherry-pick、复用其中的模型、参数、数值、结论或输出。若后续独立论证出同名方法适用，必须依据可见题面、真实数据或独立可核验文献重新建立证据链。

## 信息与证据

区分已验证事实、待验证假设和备选设想。公式与结论必须可追溯到题面、正式文献或实验；不得把 AI 输出作为学术依据。方法选型、核心推导和论证由团队完成并核验，记录 AI 实际辅助环节供论文披露。

## 数据与接口

- 原始文件只读；处理代码与生成结果分开。保留数据来源、可信度、单位、缺失规则、筛选口径与校验值。
- 实测、估算、半合成数据分别标注；训练、检验与外推边界在选模型前确定，避免泄漏。
- Q1 → Q2：输出质量评分、域映射、配比及其效应和不确定性；Q2 不重读附件 A 原始记录。
- Q2 → Q3：输出模型形式、参数、尺度、适用范围及参数不确定性；Q3 同时记录约束残差和可行性。
- Q1–Q3 → Q4：通过经验证的 Loss–Benchmark 映射连接，不把 Loss 改善直接等同于能力提升。
- 每个实验记录编号、输入版本、参数、随机种子、命令、耗时、输出、验证方式和局限。
- 论文关键数值与图表建立索引，指向对应代码、输入及实验记录。

## 排期纪律

每天 12:00、18:00 与 21:30 建议做团队短检查和集成，关键交接点即时处理；不自动创建提醒。模型工作与论文同步推进。25 日 23:00 前必须有四问完整初稿；26 日 15:00 后仅为错误或必要补证修改核心模型。落后时优先保留全部题面必做项，削减附加模型与装饰性实验。

## 三人写入隔离与记忆汇总

权威流程见 TEAM_WORKFLOW.md。三台电脑各自 clone；个人分支只有本人写入，main 由唯一集成人维护。chm/cyj/zhh 分别拥有 src、outputs、experiments、problem、interfaces 下对应小写子目录和各自论文章节。修改他人范围前先协调，不依赖 Git 自动阻止。

普通成员只改 memory-bank/members/本人角色.md，并在 handoffs/本人角色/ 新增不可覆盖的交接。六个公共文件由集成人验收后汇总。个人分支推送后队友 fetch/git show 可读；正式输入默认采用已集成版本，临时输入须记录精确 SHA 与未集成状态。已备份、已验收和已集成是三种不同状态。

接口先定义字段、单位、版本、有效范围和证据；生产者修改自己接口，消费者记录所用版本。破坏性变更发新版本并标明重跑范围，禁止静默换数据、覆盖旧结果。分支合并逐个进行，固定个人分支保留 merge 历史，不强推。

## 额度耗尽前远程同步

此为用户明确的持续要求。每次有变更的任务结束、里程碑完成和长任务启动前都做检查点。工具可用时检查相关额度窗口；任一窗口剩余 ≤20% 优先保存和推送，≤10% 暂停新重任务直到完成远端核验。无法读取额度时按更小工作块同步，不能假设能精确预知中断。

顺序：更新本人成员记忆与新增交接（集成人更新公共记忆）→ 审核待提交文件 → 显式暂存 → 提交 → 推送本人分支 → 核对 `git rev-parse HEAD` 与 `git ls-remote origin refs/heads/<当前分支>`。二者一致才算上传成功，不等合并 main 才备份。无变更时也需核验已有提交是否在远端。存在远端新提交时先整合，不强推覆盖。

失败时保留本地提交并记录错误，继续处理不依赖上传的工作，及时说明待推送状态；下次先补推。远端是公共仓库；用户本次已明确要求将 F 题原件和原始附件入库，具体清单和 LFS 获取流程见 DATA_INDEX.md。未来新增资料仍核对来源和授权，不默认扩大共享范围。

## 跨附件变量与跨分支接口规则（2026-09-23 冻结）

公共详细规范见 `TEAM_COLLABORATION_DEPENDENCIES.md`。以下规则视为系统级约束：

- **相同字段名不代表相同统计量。** 跨附件连接前必须核对定义、验证集、尺度、单位和生成机制。
- chm 的 Q1 质量评分与 B6–B8 的 `Q_score` 不得直接按数值相等连接；必须通过显式 mapping 或共同潜在坐标，并报告 mapping 不确定性。
- Q1 配比模型的 13 个 domain Loss 与 B1 泛化 `val_loss` 不得未经验证直接相加。p 的进入方式、anchor target 和尺度传递由 chm+cyj 共同冻结。
- C7 上下文长度由 zhh 作为外生情景交付给 Q3；不得把观测情景偷偷改成自由连续优化变量。
- Loss–Benchmark 桥接必须由 zhh 的可比性与误差模型控制；不得把 Loss 改善写成确定 Benchmark 增益。
- 附件责任边界：A=chm、B=cyj、C=zhh。其他成员以接口/manifest 为正式输入，除独立复核外不建立第二套正式清洗和主模型。
- 跨成员接口消费必须记录：来源分支或 main、精确 commit SHA、接口版本、文件哈希、单位、有效范围、状态（draft/validated/integrated）。
- 正式计算默认只消费已进入 main 的 validated 接口；若因排期需要提前读取个人分支，必须明确标注“临时未集成”并在上游版本变化后重跑。

## 2026-09-24：完整仓库/模型审查模式

用户要求把 `REPOSITORY_REVIEW_PROTOCOL.md` 作为全团队共享审查记忆。触发条件不限于正式“audit”一词；只要成员要求检查远程分支、本地仓库、代码、数学模型、问题设置、结果或论文结论，就必须执行完整流程。

核心顺序固定为：

\[
\text{题面}
\rightarrow
\text{可信数据字段}
\rightarrow
\text{变量来源}
\rightarrow
\text{可识别性}
\rightarrow
\text{模型}
\rightarrow
\text{验证}
\rightarrow
\text{结论}
\rightarrow
\text{跨题接口}.
\]

关键防错规则：

- 先证明可识别，再允许估计；
- 下游需求不能反向创造上游参数；
- 文献模型先核条件，再决定是否可用；
- validation/test 不回流拟合；
- support shift 与目标变量同时变化时，不得做单因素机制归因；
- estimated/半合成/插值必须与真实观测分开；
- 结果稳定、bootstrap 窄、代码测试通过都不能替代 identifiability；
- 每次完整审查必须做一次从零 red-team，避免“所有文件彼此一致但共同建立在错误前提上”。

审查结果按 BLOCKER / MAJOR / MINOR / NOTE 分级，并明确最终状态。Q1 2026-09-24 的错误是永久反例：三组经验幅度可以拟合 \(\eta\)，但附件 A 不足以识别纯 \(N\) 连续尺度律。

