# CYJ：Q3 v8 联合验收任务清单

更新时间：2026-09-26（北京时间）。负责人：CYJ；联合验收对象与 Q3 数值负责人：CHM。本文件保留开工时的任务定义；**本轮实际执行结果与未完成项**见 [`20260926-q3-v8-independent-acceptance.md`](20260926-q3-v8-independent-acceptance.md)。下方方框是原始计划快照，不再表示当前完成状态。

## 已核实的工作起点

- 远端 `integration/chm-q1-clean-20260923` 已指向 `c052b6918c3f77a2285622521d8abb1b429513be`。该提交中存在 `src/chm/q3_conditional_v8.py`、预算与假设扫描、测试、`outputs/chm/q3_conditional_v8/manifest.json`、Q3 数值章节和图。**CHM Q3 v8 已发布，不再把“等待 CHM 发布”列为任务。**
- CHM v8 manifest 自述消费 CYJ v8 文件树 `fd2dbb3b2002983430329cdb2ec6a275c2eed4f6`、CYJ 验收主体 `615c078517379284f6b0504c568790a7975c6eda`、Q1 v2 manifest SHA256 `c621f7e405106e42720f918f490235b5e8becefb0f7c8cb997736e96337117a9`。当前 CYJ 远端分支为 `046abaeede95868827fd001dcfee62eb58614eaa`；后续提交中的论文或交接不能自动改变已消费的模型字节。
- CHM manifest 已列出固定配方、87 个已观测候选联立、原生 B 质量敏感性、九个单因素假设情景及公开真实 N/D 部分外测。**这些是生产者已发布产物，不等于 CYJ 独立验收通过。**
- 上述远端 SHA 与发布文件树已作只读核对；本清单未运行 CHM/CYJ 验收命令，也未复算数值。正式开始时仍须重新核对远端、manifest 和文件哈希。

## 1. 冻结消费版本与证据（CYJ 独立完成）

- [ ] 在本人分支记录 `git status`、CYJ HEAD、远端 CYJ/CHM/main SHA、merge base 和双向差异；确认没有把 CHM 发布后的其他变更混入验收对象。遵守 `REPOSITORY_REVIEW_PROTOCOL.md` 的完整审查流程。
- [ ] 从 CHM `c052b691…` 锁定 Q3 v8 manifest、代码、输出与论文文件；逐项核验其中 Q1 v2、B1、B7、CYJ v8 输入哈希，以及 `fd2dbb3…` 文件树与当前 CYJ v8 生产者文件的关系。如发布对象更新，先另记新的精确 SHA 和差异，再决定是否变更验收对象。
- [ ] 形成 CYJ 自己的版本锁定表：生产者提交、消费者提交、逐文件 SHA256、单位、数据角色、支持范围、运行环境及日期。关键身份不同列为 **BLOCKER**，不得静默替换文件。

## 2. Q2 v8 生产者复核与科学边界（CYJ 主责）

- [ ] 在干净检出中重放 v8 冻结 fixtures，运行当前 CYJ 单元／回归测试；保存实际命令、退出码、日志、环境和哈希。历史 18/18 fixtures、95/95 tests 只作对照，不预填本次结果。除非发现缺陷，不重新拟合已冻结的 B1 五参数或 B7 三参数。
- [ ] 核对题面要求 → 字段 → 变量来源：B1 真实同源 `N,D,val_loss`、B7 半合成 `N,D,Q_score,val_loss`、A 侧 Q1 派生 `p,Q_A`、C7 外生上下文。分别陈述 B 侧 `N–D–Q` 与 A 侧 `p` 的支持；**不存在完整 `N–D–Q–p` 配对观测支持**。
- [ ] 复核 B6/B7 重叠、B8 冲突、B2/B3 半合成或插值、B4/B5 族内方向、B10 estimated，以及 A6–A11 曾参与模型取舍的事实；不得把这些写成完整四变量的独立盲测。
- [ ] 再做一次从题面和可信字段出发的 identifiability/red-team 审查：质量映射、A→B 配比桥幅度、质量与配比重复贡献和规模衰减均未由 A/B 配对数据识别；输出只能是声明假设下的条件结论。

## 3. 数学、约束与 CHM 实现的交叉核对（CYJ 理论主责，CHM 修其代码）

- [ ] 对照 `interfaces/cyj/NDQP_SCENARIO_V8.md`、`paper/latex/sections/cyj/q3_theory.tex`、CHM 的 Q3 v8 代码与数值章节，逐符号检查 Loss、质量代理、配比乘子、13 目标权重、锚点、单位、支持边界及请求拒绝规则。列出公式—代码位置和有限差分结果。
- [ ] 明确区分质量映射 `\phi(Q_A(p))` 与成本族 `g_c(Q)`；主方案的 `Q=\phi(Q_A(p))` 随 `p` 确定，原生 B 质量独立控制只属于单独敏感性模式。
- [ ] 与 CHM 对齐 `Q_0=0.5` 的角色：它是显式情景基线；主方案另设的 `Q\ge Q_0` 是**候选准入政策**，并非由成本正部公式自动推出。检查理论、代码、候选 136 拒绝示例和配方 172/477 的处理一致。
- [ ] 独立审查 CHM 对固定配方消去 `D`、在 `log N` 上求解、87 个离散候选取最小值及数值下界的适用条件；区分固定策略、离散枚举与原生 B 质量模式的最优性范围，不把局部求解器退出当全局证明。
- [ ] 若发现 CHM 实现或数值输出问题，在本人审查记录中附最小反例、受影响文件和需重跑项目，由 CHM 在本人目录修复；CYJ 不代改 CHM 源码、输出或章节。

## 4. 独立消费者复现与敏感性（CYJ 执行，CHM 对照）

- [ ] 从锁定的 CHM 发布对象复算三模式的 **4 预算 × 3 上下文 × 3 成本族＝36 格／模式**。逐格比较可行状态、`N,D,Q,p`、三项成本、总成本、预算残差、条件 Loss 和支持标记；分别报告最大绝对／相对差，而非仅报告通过格数。
- [ ] 独立检查展示格、`10^19` FLOPs 且 131072 Token 的不可行格、`p136` 的质量政策拒绝、配方 172/477 的选择、87 候选筛选依据与哈希、高预算支持上界，以及预算扫描中的状态转移括区。锁定版旧数值只作对照，差异需追因。
- [ ] 审核 CHM 已发布的九个单因素假设情景：`Q_0`、质量映射斜率、配比桥强度改变后，候选选择和转移位置是否翻转；另检查 `direct` 与 `direct_and_near` 的映射政策差异是否已有证据，缺项标为待补，不把情景跨度称为预测区间。
- [ ] 核对 CHM 的公开真实 N/D 外测只支持 **N/D 子结构的部分外测**；它没有可对齐的 A4 17 域 `p`、B7 质量 Q 和完整成本，不得用于宣称四变量真实最优已外部验证。
- [ ] 保存 CYJ 独立日志、输入／输出哈希、运行环境、随机种子、退出码、约束残差和与 CHM 的差异表。软件一致与数值精度不能替代跨附件科学识别。

## 5. CYJ 论文、合同与本人签收（CYJ 负责）

- [ ] 逐项对照 Q2 v8 章节、CYJ Q3 理论章节和 CHM Q3 数值章节的公式、成本、模式、质量政策、支持集和主要数值。CYJ 只改本人章节；CHM 数值章节若需修订，由 CHM 修改。
- [ ] 对论文每个 CYJ 引用的关键图表建立“论文 → CSV/JSON → manifest SHA → 提交”的追溯；显式写出 B7 半合成、A/B 未标定、支持域截断、敏感性翻转和公开 N/D 外测的限度。不得保留 v7 结果与 v8 公式混用。
- [ ] 在 `problem/cyj/` 或 `experiments/cyj/` 写独立验收报告，逐项记录 G0–G5 的 PASS／限定通过／FAIL、BLOCKER/MAJOR、反例、差异、科学适用边界和 CHM 需处理事项；在 `memory-bank/handoffs/cyj/` 新增交接。**只有实际完成后**才签署 CYJ 的生产者和消费者复核结论。
- [ ] 仅在证据与 CHM 签收齐备后，由 CYJ 更新本人 `interfaces/cyj/CONTRACT.md` 的条件可消费状态；`empirical_cross_source_validated=false`，旧 `ready_for_Q3` 的语义与现有消费者依赖须先查清。不得手工修改生成的验收 JSON 来消除待办状态。
- [ ] 更新本人 `memory-bank/members/cyj.md`，明确选择文件暂存、检查 diff、提交并推送 `team/cyj-scaling`，用远端 SHA 核对备份成功；把精确提交和验收结果交给 CHM 与已确认的集成人。

## 责任边界与完成判定

| 工作 | 负责人 | CYJ 的完成证据 |
|---|---|---|
| Q2 v8 模型、接口、公式和 CYJ 论文 | CYJ | 冻结版本、生产者复核日志、理论与代码对照、CYJ 签收 |
| Q3 v8 求解器、输出、CHM 数值章节和 owner acceptance | CHM | CYJ 独立差异报告；问题由 CHM 修复并重新发布 |
| 联合结果是否可签收 | CYJ + CHM 各自签署 | 两份独立签收及其精确 SHA；有 BLOCKER 时不通过 |
| 公共联合报告、main 集成与全稿编译 | 已确认的集成人 | 合入后的 main 测试、公共记忆和远端 SHA；不由 CYJ 单独宣布 |

**允许的验收结论**仅为：在冻结的 Q1 v2、CYJ Q2 v8、CHM Q3 v8、题面代理成本和明示跨源假设下，有限候选集内的 Q3 条件数值优化经独立复核。不得宣称 A/B 映射已实证标定、真实大模型全局最优、联合 95% 预测区间或 Q4 已完成。
