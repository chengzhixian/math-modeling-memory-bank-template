# 第一问图文增补与优秀论文对照复盘（2026-09-24）

参考入口：`main` 分支 `references/award_papers/README.md`。公开可核验国一索引与已实际阅读的 2023 优秀论文分开标注，不能把未经核实的 2023 单篇写成国一。参考作品只用于表达和审稿视角，不作为本题数据或模型的依据。

## 本轮已改进

- 将已有跨规模箱线图、相同配方直接排序图和 eta 校准图嵌入第一问初稿，新增七域 Q 中位数/条件区间图及配比变量消融图；图注注明数据范围、条件和解释边界。新图可由 `src/chm/q1_paper_figures.py` 从已提交汇总表重生。
- 将 arxiv/github 非重叠扩展集的代表性负相关做成正文表，读者无需翻阅所有 22 指标对输出；保留“描述性、未做多重比较”的限定。
- 模仿范文“结论旁放直接证据”的结构，让质量排序、跨尺度迁移、直接排序对照和模型消融各自对应图表；图表使用蓝—橙—灰低饱和配色，灰度打印仍可辨识。

## 按重要程度的后续方法改进

| 优先级 | 当前问题与证据 | 建议的可执行检验 | 结论使用规则 |
|---|---|---|---|
| P0 | 质量域 Q 的条件区间很窄，但去掉 RPS 家族后排序 Spearman 仅 0.8214、4/7 域变位；容易把记录抽样误差错当总不确定性 | 将三家族权重扫描的域排名范围/频率整理成正文补图或附表；主文同时展示“记录误差”和“评分规则误差” | 在完成前只称描述性质量评分，不称七域固有质量真值 |
| P0 | 1B 的 13 域排名中位数 0.7067，但完整代理的 RMSE 仅 4/13 域优于常数基线；排序与绝对预测不一致 | 拆分报告排序检验、校准斜率/截距、绝对 Loss 误差；针对不同配方支持集做可比子集或重加权敏感性分析，不能用当前 1B 反推纯规模效应 | Q2/Q3 只能把 p 用作带尺度不确定性的情景修正 |
| P1 | 22 个指标的方向由模型评分锚点导出，可能把锚点偏好循环写回综合 Q；book 样本仅 171 条 | 试验去掉模型家族后由外部可核验任务质量或人工小样本盲评锚定；对 book 给出样本数和抽样代表性说明 | 目前不将 Q 等同于训练收益或人工偏好 |
| P1 | A2/A3 的 A1 样本记录包含在扩展集中，已用非重叠记录纠正；冲突强度只按观测相关排序 | 对入选指标对做分层 bootstrap 区间与多重比较；检查相关符号在两个域/独立子样本的稳定性 | 正文冲突表仅示例，不写显著性与因果结论 |
| P1 | A16 仅 6/17 direct 或 near-direct，B `Q_score` 与 A `Q_z` 无配对标定 | 等 cyj 的 B 侧接口和锚定数据；若无成对样本，继续分轨陈述，避免人为补 17 维真值 | 第一问可独立成章，但不能据此生成统一 L(N,D,Q,p) 实测结果 |
| P2 | 当前图的字体为英文、沿用独立脚本版式 | 集成人按正式模板统一中英字体、编号、版心与图注，并输出 PDF 校验缩放后的可读性 | 不影响本轮数值结论 |

本轮仅修正论文证据呈现，没有为追求范文外观而改变评分权重、Ridge 超参数或数据类型。上表方法项尚未运行，不能在论文中写成已验证结果。
