# Q4核心补强v3设计冻结（先设计再运行）

范围：用户2026-09-26授权补核心工作，不写论文、不修改paper/或LaTeX。输入冻结为v2 CSV、原始C3/C4/C6及固定Q3快照；结果写入独立 `outputs/zhh/q4_core_v3/`，不覆盖旧结果或假装论文已同步。

1. 历史贡献：以综合能力均值为分解目标，按原始model_class与固定logN网格（0.5 decade主，0.25/0.75敏感性）确定早末共同支持。对模型类型及可选共同开发者固定权重，在参数格内做对称分布/条件均值分解：规模分布变化与同格能力变化严格相加得到标准化增长；全样本与标准化增长之差单列组成/支持差，不称技术。同格时间变化只有“未控制因素不变”的条件技术解释，报告格内logN差、覆盖和稀疏性；近零分母不给份额。
2. 前沿：以最近两个月同类型S q90为预测目标；个体logit q90线性分位回归用线性规划精确求解，比较常数、直接两月前沿趋势、q90资源动力学和旧均值动力学。滚动测试采用训练截点之后完整的两个自然月，无已知测试月参与拟合；按历史版本去重及历史C4资源截断。主资源动力学仍保留局部分布平移的条件假设，检验结果决定如何评级，不预设它优于基线。
3. C4：相同审计条件下主/宽比值实际重估年度资源增速、N/D配置及全部前沿情景。情景范围逐离散假设给条件bootstrap分位，再取这些分位和不同模型候选的min/max；明确是“条件范围的情景并集”，没有情景概率或预测覆盖率。
4. C6：不把medium当共同坐标。逐原Loss_Source分别拟合单调Loss及常数，标明validation/training/unspecified；primary只允许明示validation或high来源。比较实际消费的单调模型LOO；high失败照实保留。Q3坐标关系未识别，采用显式仿射坐标scale/offset与上游Loss扰动网格作压力测试，报告支持/不可行及同坐标假设下的能力差/排名，不标实证标定。
5. C3：分Source/Year实际计算任务与参数统计，检查Average与六任务均值差、历史零值及同名重复，禁止异构Average跨来源连成一条进步曲线。

数据均已被查看；所有模型比较称诊断，不能称盲测。贡献格数下限2/期；严格开发者控制敏感性允许无可用格并显式不给估计。不把无法识别的量借下游需求补造。Bootstrap种子20260927，开发者块80次；不作为新未来随机冲击或校准95%PI。

验收：三项分解闭合、支持外/不可行不换算、精确分位目标/手工反例测试、回测窗口独立、C4宽门禁确实重算、来源Loss不跨坐标混合、有限输出与输入代码哈希、只改zhh核心目录；更新成员交接并提交/推送/SHA核对。
