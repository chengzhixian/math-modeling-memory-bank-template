# Q2 可视化复核记录（2026-09-26）

范围：main @ a19039b 的 Q2 v8 结果再展示。没有完整重跑 Q2 训练、Q1 上游或所有仓库验收；不把本次展示检查称作模型验收。

SciPilot 先剖析 B1 原始表和四张局部结果表，再按现有论文论证拆成 7 图。B1 有 1,176 行、8 种规模；B7 外层验证 9 / 5 / 10 折；弹性、同 Loss 权衡、桥敏感性各只有三个情景。小样本使用逐点图；不作均值柱，不虚构误差棒。17 域的136个配对用有向矩阵。

复算冻结 B1 公式的同源 RMSE，断言与发布值 0.00014657641916440922 一致；同 Loss 权衡仅采用 `root_in_support` 的新质量解。旧质量等价对比在 N=10B、delta=0.1 下无支持域根，不把这个缺失值画为零。转移矩阵逐行核对 CSV 的 donor→recipient 方向，并核对全部局部双向可行；不使用离开单纯形的 ambient cross partial 充当可行协同效应。

关键展示数字：q+0.05 时 N=0.1/1/10B 的同 Loss 新规模约下降 8.6/12.4/14.5%；q+0.1 时约下降 16.4/23.3/27.4%。桥压缩/默认/扩张 Loss=2.3853/2.3443/2.3034。前者是固定配方的假想质量干预，后者是桥假设敏感性，均不能解释为实测因果收益。

实际读图发现并修复：Microsoft YaHei 不含部分上下标字符，改用数学字体；17 域 x 轴标签改90°；浅色等值线加白底深字，去除几乎在角落的2.1线；权衡图图例放到绘图区上方。灰度弹性图中三个标记仍能区分。最终布局检查均为空，PNG与SVG无检查问题；PDF字体警告通过直接提取嵌入字节排除。所有导出为7.2英寸宽，PNG300DPI，PDF/SVG为矢量。

科学限制沿用 main：B1同源，B7半合成，跨源桥未标定，重复计数未识别，Q1负Loss代理仍需修复。未修改任何模型结果或公共记忆。
