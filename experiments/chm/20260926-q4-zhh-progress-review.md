# Q4：zhh 远程进度与继续工作条件检查

## A. 范围与版本

用户本次要求判断是否可开始 Q4，并查看 zhh 已做到哪里。本轮只读检查 zhh，不修改、合并或推送其分支；不是 Q4 新模型发布。

- CHM 起点：`9228507cc931d79afbdb92f9218e339f09f59e2d`，工作区干净。
- fetch 后 zhh：`2d91c662da586db3792db2a36266249d8919c7ba`，最后提交 2026-09-24 20:41:14 +0800。
- main：`f9693bbf4c205aa46d3719f6f8a1d6f26561d05f`。
- merge base：`968ef7a36f1503aa222a0b09c8a8cb5d0f535ecd`；zhh 相对 main 20 behind / 5 ahead。
- zhh 独有修改集中于本人代码、结果、章节、接口和记忆；main 从共同基线至今未修改 zhh 范围。本轮不自动整分支合并，以免覆盖 CHM 已更新的 Q1/Q3。

可信来源：当前题面 DOCX、可见数据说明 `problem/readable/DATA_DESCRIPTION_VISIBLE.md`、真实 C 附件、精确 zhh Git 对象。隐藏页边文字、作废 Gemini 工作及撤回预测均禁止复用。

## B. 题面与数据追溯

| 要求 | 字段/数据 | 目前结果与缺口 |
|---|---|---|
| 综合能力、筛选、模型类型、时间 | C1/C2 六分数、参数量、Type、Epoch 状态、许可证、提交日 | 已有六项等权均值、严格/扩展开放集、两类模型与时间轴；许可证代理不等于已验证研究许可 |
| 规模与非规模贡献 | C1/C2 参数量、日期、类型 | 已做 OLS 条件关联及窗口分解；不是因果技术贡献，尚未成为动态前沿模型 |
| C3 使用 | Year/Params_B/Average/Source | 已生成年度分位及最大值；异构评测只作背景，不能直接拼接 |
| C4 算力、数据量、开放权重 | Training compute (FLOP)、Training dataset size (total)、Open model weights?，另有 Parameters | 代码使用算力/日期/开放性；没有读取训练数据量，未完成本项全部要求 |
| C8 逐任务 | JSON results 中 BBH acc_norm | 已完成 1860 模型×24任务聚合；4 个损坏 JSON 被记录 |
| Loss→Benchmark | C6 Val_Loss/N_params_B/六任务/Loss_Source/Loss_Comparability | 7 high、68 medium；已有分级拟合与失败诊断，当前接口 unidentified |
| 放缓情景未来 12/24 月前沿及不确定性 | C4 资源演化、C1/C2 能力及桥接 | 当前预测数组为空，尚未作答完成 |

变量来源：S 为六项均值 derived，N 为 C1/C2 十亿参数 observed，t 为提交日相对 2024-06 derived，I 为类型 derived。OLS 系数只在选定函数族/筛选下条件估计；时间系数不是技术因果效应。C4 compute、N、D 是各自汇编记录，字段非空不保证同一训练阶段或文本 token 单位。未来增长率与跨坐标映射应标为 assumption/scenario，不能写成观测估计。

## C. 复跑、质量、角色与适用边界

从固定提交 archive 出独立忽略目录 `.upstream/zhh-q4-review-20260926`，只在该快照复跑：

`node .upstream/zhh-q4-review-20260926/src/zhh/q4_analysis.test.js`

命令 exit 0；复跑 `q4_results.json` 与该提交发布对象 JSON 完全相等。并独立读取 CSV 检查：C1 4576 行，模型名重复 79 行，六任务无空字符串；C6 75 行、模型名不重复、等级 7/68；C8 聚合 1860 行全部 taskCount=24。没有做危险的仅模型名跨附件 join。

C4 3523 行，数据量字段非空 1424 行；直接可解析为正有限 compute、Parameters、dataset size 的完整案例 850 行，其中 Open model weights?=yes 的 432 行。这些是字段完整计数，不是合格同坐标 LLM 样本数，必须再筛 Domain/Task、架构、日期、训练阶段、token 单位、估算置信度。现有数据已有候选资源链，不能断言必须新增数据才能继续。

数据角色：C1/C2 全样本描述和拟合、时间 80/20 留出诊断；开放性/类型/去重是已查看过的敏感性，不是独立最终盲测。C6 Loss 排序和来源留出为桥接诊断；高可比 7 行来源为附件 B 的 Pythia 最终 checkpoint，不能再冒充独立验证 Q2 的数据。C3 历史尺度不同，不与新六任务轨迹直接拼接。C7 为外生情景，C8 为描述聚合。

文献适用：当前脚本为基础 OLS，没有可验收的因果识别设计或新动态模型；本轮不引入未经核验的文献公式。数学/代码均为 S=beta0+betaN log10(N)+betat t+betaI I；OLS 正规方程仅含 1e-9 数值对角稳定项。Bridge 为 S=gamma0+gammaL Loss+gammaN log10(N)，medium 权重 0.35 是设定，不能当识别结果。代码 number('') 会返回 0，是潜在缺失解析问题；本次 C1 六任务没有空字符串，未据此断言当前已发布六任务数值受损。

基线/稳健性：已有严格开放集、类型分组/交互、模型名最新行敏感性和时间留出；现有共同斜率不稳。未来前沿模型仍需常数/趋势/分位基线比较、滚动时间诊断及情景不确定性，不可把一个 RMSE 直接加减成 95% 区间。7 行 high 桥接 Loss 系数为正，且跨来源验证失败，不能预设 Loss 越低必然直接换算为指定分数提升。

## D. 分级发现与下一步

### BLOCKER（完整 Q4 答卷）

1. `forecast.predictions=[]`，`frontier_forecast.csv` 点值及区间为空；算力放缓预测的题面要求未完成。需要明确动态/前沿定义、放缓情景、预测起点、12或24月结果和区间。允许声明假设后求条件预测；不能把未识别的因果量写成实证事实。
2. Bridge 接口仍 unidentified；Loss 排序留出 RMSE=7.060、R2=-1.376，来源留出 RMSE=9.887。不能把 Q3 的 Loss 直接代入现有换算得到能力增益。需要从同坐标子集重新判断映射，或经可核验文献支持建立声明假设的映射，并传播误差/敏感性。

### MAJOR

1. C4 数据量字段未使用。补充资源字段/单位/样本角色审计后重跑资源链和预测，章节补结果。
2. 43.85%/56.15% 是窗口关联项绝对值归一，窗口中规模项为负；不能称为真实增长的因果技术占比。应明确分解对象、符号、模型假设及类型/时间混杂，再形成赛题所需动态或因果框架。
3. 现有回归描述样本条件均值，参数95%分位与时间特征组合不等于能力前沿。预测需单独定义前沿并匹配验证对象。

### MINOR / NOTE

开放许可证研究与复现权限、数值缺失解析、C3 与新评测版本异质性应明确。上述未完成项不要求先补采真实训练；先充分审计现有 C4 及 C6，再决定是否需要权威外部资料。

## E. 从零反向审查与结论

只看题面与字段：C1 支持能力和规模短窗关联，C8 支持任务差异，C3 提供混合历史背景，C4 同行存在资源字段，C6 提供分级但稀疏的桥接。它们不自动支持技术因果份额或无条件未来前沿；模型类型、后训练、提交选择和评测口径可以解释时间项。合理做法是保留已验证描述，明确假设构造可检验的动态和预测情景；Q3 完成不会自动改善 bridge。

**可以开始/继续 Q4；已发布描述与诊断结果可复现，完整 Q4 当前 NOT READY。** 本轮不修改 zhh。CHM Q3 v8 的公式、支持域、主结果和九场景敏感性现可供后续消费，消费者须固定 CHM 发布 SHA；没有联合统计预测区间，不得伪装成上游 95% CI。
