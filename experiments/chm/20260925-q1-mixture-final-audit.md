# Q1 第三小问配比审计阶段检查点

日期：2026-09-25；状态：阶段结果，非 Q1 最终冻结。原始 A4--A15 配比/Loss 表已按 index、列序与文件 SHA 独立核对，估算数据仅用于压力测试。

## 本轮实现与复现

`python src/chm/q1_mixture_final_audit.py` 生成 `outputs/chm/q1_mixture_final/`：12 文件逐个 SHA/index 审计、归一化偏差、A6/A8 逐配方一致性、13 目标的 RMSE/MAE/bias/Spearman/训练均值基线/组内中心化诊断、A4 凸包成员性与最近邻距离、训练内五折选择的10项交互候选对照。

## 已核验结果

- A6/A8：相同 index 下的 256 个配方17维逐项相同；对应真实Loss可作配对比较。
- A10 1B：64 配方中17个位于A4训练512配方凸包，47个超出；凸包内也仅代表几何插值，不代表已真实训练过该点。
- 原 Ridge 在1B绝对 RMSE 上仅4/13目标优于训练均值基线；13目标逐项误差与偏差见表。排序能力、中心化相对Loss与绝对Loss必须分开解释。
- 交互候选：仅以 A4 配方方差选5个域，加10个二元乘积项，在A4+A5五折选择正则；三个真实检验组的RMSE比主Ridge分别11/13、13/13、13/13目标更低；在1B相对训练均值基线为11/13。这个候选显示可能存在非线性预测收益，但不证明二阶因果协同，也未替换正式13×17 Ridge接口。主模型转换与模型选型须由参赛者理解、复核。
- A12--A15 63个配方来自A4已见配方，是 estimated/extrapolated 尺度压力测试，不是真实10B/70B独立观测。

## 仍需完成

明确条件多目标情景、17域质量覆盖与新不可变接口；将候选的预测增益与当前主模型稳定性/解释性取舍写入论文；生成最终复现和消费者自检。不得用候选的1B改进掩盖仍然明显的跨规模绝对Loss偏差。
