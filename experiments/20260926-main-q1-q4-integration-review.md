# Q1–Q4 远端分支再集成审查（2026-09-26）

状态：**VALIDATED FOR STATED SCOPE**。本记录审查的是四问当前有效成果能否组成 main 的可复现交付，不把条件模型提升为已识别的跨附件因果规律。用户本轮要求先完成四问集成，论文编译和排版不在本轮验收范围。

## A. 范围、冻结和来源（Stage 1–2）

集成基线为当时远端 `main@ace2bf0833fee186a0fb2d2940923d1ccaecac4f`，专用分支首个远端检查点为 `codex/integrate-q1-q2-q3@863792e54eb3f9eb5c6da6993e685ee4126fe0b5`，两 SHA 已经 `ls-remote` 核对。来源：Q1/Q3 `integration/chm-q1-clean-20260923@c052b6918c3f77a2285622521d8abb1b429513be`；Q2 的当前远端为 `team/cyj-scaling@d5e05cedeba3b86abe4ab624239504a21d1abee4`，正式 v8 数值生产者为 `fd2dbb3b2002983430329cdb2ec6a275c2eed4f6`；Q4 `team/zhh-frontier@ee23b200e9de7f78477d945b186233741bd3b8fd`。论文与数值可能出自不同来源提交，未将它们冒充同一原始发布包。单一集成人在隔离工作树操作，原成员分支未改写。

可信来源为 `problem/F/` 的官方题面、清理后的《数据说明》可见页面和 `problem/readable/DATA_DESCRIPTION_VISIBLE.md`、已校验的原始附件，以及逐问注明的冻结参数与接口。清理 PDF SHA256 `daa8479decf578ec5f0b165683ac5715fed445ba8c148f386e7b071f5c27dfdc`；`scripts/verify_raw_data.ps1` 对 2,014 项、564,436,312 字节报告 PASS。禁止来源为历史 PDF 隐藏页边文字、作废的 Gemini 提交、未验收 legacy 模型、无法追溯的本机缓存和聊天中的未验证数字。

## B. 题目要求、数据和变量（Stage 3–5）

| 要求 | 输入、字段和单位 | 数据角色与足够性 |
|---|---|---|
| Q1 质量及冲突 | A1–A3 的 22 个质量信号和领域/样本 ID；分数 `Q_A` 无量纲 | A1 冻结方向与标准化，A2/A3 去重后扩展核对；只能形成工程质量代理与指标分歧，缺人工真值 |
| Q1 配比与 Loss | A4/A5 512×17 配比 `p`、13 个目标域验证 Loss；A6–A11 的真实规模组；A12–A15 estimated 表 | A4/A5 训练和 CV，A6–A11 曾参与模型形式比较，A12–A15 仅外推压力；A 侧相对效应有条件可估，跨规模纯参数不可识别 |
| Q2 规模、token、质量、配比 | B1 的 `N_params_B,D_tokens_B,val_loss`，N/D 为十亿单位；B7 的 `Q_score` 半合成质量；A 侧 `Q_A(p),r_w(p)` | B1 同源 N/D 基线、B7 半合成质量条件式可估；A/B 无同条件成对标定，质量与配比桥仅情景 |
| Q3 预算联合配置 | Q1/Q2 冻结接口；题面成本 `C`（FLOPs）、C7 `L_c`（token），预算 `10^19/10^22/10^24` | 成本函数是代理，Q3 是给定政策、支持域、87 个观测配方内的条件优化；非真实训练的无条件最优 |
| Q4 技术贡献与预测 | C2 Benchmark/日期/类型/开放代理，C3 历史任务，C4 N/C/D 资源，C6 分来源 Loss，C7 上下文，C8 逐任务 JSON | C2/C3/C4 历史描述和条件前沿；C6 来源坐标与 Q3 Loss 无可识别的实证映射；未来区间无覆盖率校准 |

核心变量追溯：`Q_A` 是 A1 派生评分而非人工观测真值；`p` 是 A4 实测组成且和为 1；`r_w(p)` 是 A4/A5 逐目标拟合 Loss 对参考配方的相对变化；`N,D` 由 B1 观测并作为 Q3 决策；`Q_B` 只在 B7 半合成表中出现，`q(p)=phi(Q_A(p))` 是明示单调情景；`L_c` 是 C7 外生情景；`Q_0=0.5`、桥接斜率、配比桥幅度、Q4 前沿 `eta/retention` 和 Q3→C6 仿射坐标均是情景参数。`Q_A→Q_B`、A 目标域 Loss→B 总体 Loss、格内时间→纯技术贡献、Q3 Loss→C6 能力坐标均为 **not identified**；代码与答案按情景或拒绝实证换算处理，未进入“已识别”接口。

## C. 数据角色、泄漏和支持（Stage 6–7）

- Q1 A1 学评分规则，A2/A3 按冻结规则检验并处理与 A1 的 ID 重叠；A4/A5 训练配比，嵌套 CV 选模型；A6/A8 有相同 256 配方，A6–A11 参与过模型形式判断，故不是最终盲测。1B 有 47/64 配方在 A4 凸包外。A12–A15 是 estimated，不能作为真实外部验证或回流训练。
- Q2 B1 八规模组留组检验是同源重构；B7 的 24 外层折来自半合成 450 行；B6 与 B7 嵌套，B8 反向质量趋势隔离，B10 是 estimated，B4/B5 只作同族方向对照。Q2/Q3 使用 B1/B7 的共同 N/D 支持，不混用旧 v7 的 D 上界。
- Q3 固定配方 172 与 87 个质量可行 A4 观测候选分别报告。公开 N/D 外测只测规模主干和部分预算排序，不测跨附件 Q/p 桥。Q4 的四个两月回测窗口重叠，C3 不同来源分开，C6 `Loss_Source` 分开；C8 损坏文件登记。不能把重复轨迹或相异 Loss 坐标当独立同分布测试。

## D. 模型、代码、数值与验证（Stage 8–13）

Q1 的 13 个交互模型以 17 个单纯形比例和 10 个乘积项预测目标 Loss。简单 Ridge、常数模型、质量方向重抽、estimated 外推与重拟合选方均已对照；完整输入、参数、单位、随机种子和命令见 `experiments/Q1/CORE_VALIDATION.md`。从 A4/A5 重拟合与发布系数最大差 `6.14e-13`，CV 最大差 `2.22e-16`，A6–A11 发布 RMSE 最大差 `4.44e-16`；55 条复制性 BH-FDR 负相关边、四个凸包数值间隙 `<0.001`。浮点分支定界不是严格区间数学证明，冲突也不是因果冲突。

Q2 代码把 B1 主干 `E+AN^{-alpha}+BD^{-beta}` 与 B7 增量 `(1-Q)G(N,D)` 分开拟合；A 侧的 `exp(r_w(p))` 是外加条件桥，未借文献名义声称已由本题识别。B1 1,176 行、B7 450 行、24 折、136 个领域对在集成主链复现；B7 三轴留级 RMSE 为 `0.048738/0.048751/0.048639`。18 组 v8 接口样例通过。B4/B5 跨家族绝对 Loss 可比性未建立，保持空值或方向验证。

Q3 的 `C=D[(6e18+2e14 L_c)N+1e9(g(Q)-g(Q_0))_+]` 与代码逐项一致。给定当前正参数和支持域，固定配方先利用 Loss 对 D 的单调性消元，再对 N 边界、驻点与数值下界检验；联立枚举 87 个配方。三种模式各 36 格的可行数为 `30/33/33`；161 点扫描与细化给出固定 33、联立 49 个转移括区。`L_crit=30000` token 是代理成本代数结果，不是观测能力阈值。九个桥接情景、公开真实 N/D 的部分外测与反例均已保留。`direct` 映射全预算对照和公开源独立原始下载未完成，不据此提高主张等级。

Q4 当前 v3 以 C2 的 2616 个去重开放代理模型、C3 的 4599 行历史表、C4 的 3523 行资源表和 C6/C7/C8 为输入。81/93 两套 C4 门禁、C3 79 个重复键、C8 4 个坏 JSON、共同参数格与开发者支持不足都显式审计。历史标准化变化严格分为参数分布和格内时间；时间项仍含 D、后训练、选择和评测。前沿与常数、趋势、均值资源作同目标短窗对照；后训练 q90 模型四个重叠窗 RMSE `2.199`，R² 仍负。Q3→C6 的坐标转换只作 4,860 行仿射/上游误差压力，正式接口 `ready_for_empirical_Q3_conversion=false`。当前 v3 45 项输入/代码/输出哈希门禁及 13 项核心测试通过。

数学与数值安全：配比非负且和为 1，N/D/Q/预算的范围、正数、有限值和原生/代理角色均由接口及测试校验；缺失/非有限/损坏原始字段被排除或记录。Q1 对 A1/A2/A3 的重叠、Q2 B6/B7 的嵌套、Q4 C3/C4 的重复与门禁均留有逐项证据。各模块的论文公式只在上述声明假设下解释，未把优化器返回值当作结构识别。

## E. 结论等级、跨问接口和从零反证（Stage 14–17）

Q1 质量和冲突为 L1 描述、A4 支持内配方为条件预测/决策；Q2 B1 同源规模关系为有限支持内 L2，A/B 桥仅 sensitivity；Q3 为给定模型的条件最优，不是观察到的因果干预；Q4 历史分解为 L1，未来范围为未校准情景，跨 Loss 能力换算不发布实证分数。没有四问的 L4 因果或 L5 域外普适主张。

接口顺序是 Q1 `chm.q1.v2.0` → Q2 `cyj.ndqp.scenario.v8` → Q3 v8 条件预算格 → Q4 v3 条件压力。Q1 原九文件字节与冻结清单保留，main 只读映射到 `outputs/Q1`；Q2 主入口不再从 v7 迁移包导入；Q3 主入口直接消费 main 的 Q2/Q1 模块和哈希，无 `.upstream` 或历史 `git show`；Q4 `prepared` 仅收六个当前 v3 实际读取的输入及清单。原来源 manifest 与 main `curated_manifest.json` 分开，后者记录路径适配后的当前文件身份。

从零只看题面和原始表头，最多能独立推出 A 内评分/配比关联、B1 的 N/D 同源经验律、B7 半合成质量条件项、C 的历史能力/资源描述。无法仅凭这些表识别 A/B 质量坐标、纯技术时间效应、Q3 Loss 到 C6 的同一损失坐标或未来覆盖率。固定质量、取消配比桥、不同支持域和不同前沿候选都是合理反事实；当前正式答案已按此降低主张并保留压力表。

## F. 分支差异、遗漏清理和复现（Stage 18）

相对基线 main，CHM 的 merge base 为 `968ef7a`，main/CHM 分别多 25/149 个提交；CYJ 的 merge base 为 `f9693bb`，多 5/103；ZHH 同一 merge base，多 5/12。三分支不能直接整体合并：Q1/Q3、Q2 v8、Q4 v3 的正式产物分散在不同目录，且混有旧版、阶段性与重复文件。逐问选择当前生产入口并递归补齐自有依赖、输入、关键结果和接口；Q4 v2 数据整理模块中仍被 v3 实际调用的有效功能被保留，旧 v2 论文和发布包不作为 Q4 当前数值结论。没有把成员历史目录、缓存、虚拟环境和被撤回模型复制到新交付。

干净工作树由集成检查点新建，没有 `.upstream` 或 `.deps`，原始资料全量校验 PASS；按 `scripts/requirements-integrated.txt` 的版本提供独立 Python 依赖后，Q1 主模型从 A4/A5 重拟合与发布一致；Q2 B1/B7/v8 主链、18 组接口样例可运行。完整命令分列在 `experiments/Q1/CORE_VALIDATION.md`、`experiments/Q2/README.md`、`experiments/Q3/README.md`、`experiments/Q4/README.md`。`scripts/refresh_main_manifests.py` 校验四问当前清单。运行时可再生中间表在忽略的 `data/processed/`；源发布哈希与 main 当前哈希的区别有记录。

干净复现确实发现两处工程缺陷，并在交付分支修复回归：Q3 `csv.DictWriter` 在 Windows 默认 CRLF，虽然数值相同，却使 Q4 读取的上游冻结网格 SHA 不符；改为显式 LF 后恢复原发布哈希 `9e1ea053...`，Q4 预处理通过。Q4 v3 生成清单曾遍历整个输出目录，第二次运行会把 main 的 `curated_manifest.json` 纳入自身清单，导致非幂等和循环身份；现排除该集成清单，重新运行仍保持 45 项哈希。两处只修复文件序列化与发布清单，不改变模型公式或核心数值；Q3/Q4 清单已按当前代码重生。

合入后从全新 main 工作树复核时，Q1 `reproduction_audit.json` 与 Q3 `ANSWER.md` 的 Git 文本检出为 LF，原精选清单却记录了集成工作树的 CRLF 工作字节。两文件内容和 Git blob 未变；将 Q1/Q3 `curated_manifest.json` 改为记录 main 实际 LF 检出 SHA256 后，四问清单只读校验通过。原来源冻结清单保持不变。

**仍需外部证据而非代码修补的问题：** A/B 配对标定、Q3 真实四变量联合训练验证、Q4 纯技术因果份额、Q3 Loss→C6 实证坐标及未来区间覆盖率。它们不阻止声明范围内四问集成，但阻止无条件科学主张。论文 PDF 编译、排版和当届提交格式由用户明确留待后续。
