# Q2 v7 requirement and red-team audit (2026-09-25)

Scope: the visible F question DOCX, visible data description, signed Q1 v2 derived output and frozen CYJ B models. The historical hidden PDF text and discarded Gemini work contribute no assumptions, coefficients or thresholds. This record audits the **conditional** Q2 answer; it does not validate the A/B bridge.

| Official Q2 item | Variable/data role | Implementation and result | Evidence ceiling |
|---|---|---|---|
| Classic N,D scaling; B1 primary | N,D in billions; B1 real Pythia same-source trajectory | `paper/latex/sections/cyj/q2.tex`, `outputs/cyj/classic/classic_fit.json`, v7 frozen B7 coefficients | B1 near-exact in-source reconstruction; data-generation provenance remains uncertain |
| B2 or B3 trajectory check | B2 semi-synthetic, B3 interpolated | `outputs/cyj/diagnostics/b2_b3_shapes.json`: all 7 B2 and 8 B3 trajectories end below start | Shape only; not an independent experiment |
| B4 and B5 cross-family/literature check | Distinct Loss coordinates not established | `outputs/cyj/diagnostics/b4_b5_comparability.json`: within-family/source size-dominance concordance 131/133 and 113/113 | Directional description, no pooled external RMSE |
| Quality in generalized law | Q_B is B7 native semi-synthetic coordinate | `src/cyj/ndqp_scenarios_v7.py`, `outputs/cyj/q2_v7/model_coefficients.json` | B7 same-source held-level results only; B6 nested in B7, B8 conflicting |
| Mixture p and A-side quality | 17-domain simplex; Q1 v2 13 target relative effects; A1 mapped Q_A screens p | Q1 signed manifest `c621f7e4...`; `policy_solutions.csv`, `quality_mapping_sensitivity.csv` | A6–A11 informed Q1 form selection; not untouched final test. Q_A is not Q_B. |
| Joint N,D,Q,p expression | B7 Loss multiplied by exp(r_w), lambda=1, eta=0 | `model_definition.json`, `baseline_model.json`, `paper/latex/sections/cyj/q2_v7_tail.tex` | Explicit cross-source hypothesis; no paired A/B empirical calibration |
| Marginals, elasticity and Q/scale substitution | Signed derivatives per billion; Q_B in native units | `marginals_elasticities.csv`, `quality_vs_scale.csv`; at N=1,D=100,Q=.5, equal observed p: N gradient -0.134643, Q gradient -0.329915; eta=0 Q+.1 equivalent old-Q N'=1.28858 B | Model-internal. Equal-loss direction fixed by the printed equation; no measured cost ratio. |
| Domain substitution/complementarity | Feasible zero-sum recipe-to-recipe directions | `domain_transfer_effects.csv`, `domain_complementarity.csv` | Directional fitted surface, not causal domain interactions; no all-pairs intervention evidence |
| B9/B10 large-size discussion | B9 metadata; B10 estimated Loss | `outputs/cyj/diagnostics/b9_b10_extrapolation_audit.json` | Out-of-support stress, no actual B10 holdout |
| Estimation and validation | Frozen Q1 v2 and B7; source-separated checks | `validation_by_source.csv`, Q1 signoff with stale bounds-file hash disclosed, B7 nested validation, 84 CYJ tests, 21 v7 fixture pairs | Neither source validates the cross-source bridge |

The alternative no-transfer model is lambda=0: p has no effect, all supported mixtures tie, and Loss equals B7. The alternative linear bridge has the same within-policy p ordering when its factor stays positive, but different Loss levels. Their agreement on ranking is an algebraic identity, not evidence of transport. Changing Q_A policy changes the feasible set, and no A-to-B quality conversion or double-counting correction can be estimated from available pairs. The numerical CHM hull gaps are below 0.001 in relative effect for four published policies; the current bounds bytes have been reproduced by CYJ because the older CHM signoff audit hash is stale. CYJ has also produced separate arxiv and pile_cc numerical continuous-hull gaps below 0.001. These are floating-point bounds, not interval-arithmetic global proofs or CHM owner signoff.

At fixed p, baseline N/D/Q_B elasticities equal B7; quality–scale substitution is a B7 identity. Eta=0.2 scenarios depart from this identity, illustrating assumption dependence. The lambda/eta grid is not a confidence interval. A joint prediction interval is unavailable. B7's source-specific empirical intervals in the earlier section cannot be multiplied into a calibrated joint interval without a justified error dependence model. A cross-source test needs matched tokenizer/evaluation set, a common reference and changed mixture, controlled N/D/Q_B and replicates, then preregistered predictive error and rejection thresholds.

Q2-specific software checks pass. The separate `finalize_q2_v7.py` checker sets `q2_implementation_complete` and `q2_answer_complete_under_stated_assumptions` only after deterministic rebuild, independent formula recalculation, current bounds reproduction, 21 request/expected fixtures (including upstream identity corruption), all 84 CYJ regressions and Q2 layout checks. A 200-draw joint-B7 `(N,D)` cluster bootstrap has been propagated at fixed p and bridge; its percentiles exclude Q1 and cross-source uncertainty and have no joint-coverage claim. CHM Q3 consumer acceptance and integrator main merge are separate gates. The whole paper still has two CHM Q1 overfull lines and unresolved bibliography citations. None of those should be silently presented as a completed final submission.
