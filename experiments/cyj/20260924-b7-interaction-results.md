# B7 质量交互候选：预注册算法的探索性比较

协议先于本次运行写入 `20260924-b7-interaction-preregistered-protocol.md`，SHA256 `bf6aad298e36e60c41877dd09c898f51b08e558a7c0da23b2bb426c2f05c2e4e`。但是候选发明受到 Task 3 全量 B7 诊断启发；因此 **24 折不是未触碰的最终测试**，结论限 B7 半合成条件层。

## 复现身份

- 输入：B7 450 唯一 N/D/Q 坐标，SHA256 `880fd265ca3e1d9bc93b4559af7ed7c18f89040634266c50bae03d88486e0f3a`；B6 子集排除，B8/B1/A 均不参加。
- 代码 `src/cyj/compare_b7_quality_interactions.py` LF 标准化 SHA256 `a32109a9b0c9e49391aa03ec804011be3520d8079c30c89fbe0114dbd72a93eb`。Python 3.12.14 / NumPy 2.3.5，ND 组 bootstrap seed `20260924`。
- 命令：

```powershell
& 'C:\Users\muyehuangyi\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -B src/cyj/compare_b7_quality_interactions.py
```

- 输出 `outputs/cyj/quality/b7_interaction_comparison.json` SHA256 `1d28169ce10f5bc274a82037320fdf003f7e3d2148eff2be91d3c3618708925b`；逐外层折的内层分数、失败、各候选外层分数、逐点残差、参数与梯度、100 次组 bootstrap 样本均在文件内。

## 已运行结果

外层 9 个 N、5 个 D、10 个 Q 留级，共 24 折；每个外层训练集上，同轴内层留级选候选。22 折选 `Q_x_logN_logD`，2 折选 `Q_x_logN`。下表是各候选在固定的外层折上直接重拟合后的**同源探索性**平均 RMSE；选中算法的分数另列。每个候选共用训练折估计的 no-Q `alpha/beta`，与原旧 constant-G 联合拟合数值定义不同。

| 外层轴 | no-Q | constant-G | Q×logN | Q×logD | Q×logN+logD | 内层选中算法 |
|---|---:|---:|---:|---:|---:|---:|
| N 等级 | .115812 | .058231 | .052300 | .057920 | .051936 | .051936 |
| D 等级 | .118348 | .057091 | .050380 | .056955 | .050207 | .050051 |
| Q 等级 | .118794 | .057617 | .050416 | .057437 | .050061 | .050061 |

选中算法的轴均值 MAE 分别 `.041161/.039152/.039024`，预测减观测的均值偏差分别 `-.003766/-.003038/.000089`。所有 24 个选中折在完整声明矩形四角的 Q 梯度均不变成正值。五候选的 100 次 ND 组 bootstrap 都拟合有效，选中双交互族的 `gN` 经验 2.5–97.5% 分位 `[-.05954,-.04770]`，`gD` `[-.02171,-.00423]`；这些仅是条件样本变动，不含模型发明/半合成生成/真实实验差异。代码同时保存每 N/D/Q 等级 RMSE、参数与梯度明细；不把 bootstrap 分位写为总预测区间。

全 B7 两阶段拟合的双交互式在网格上 `G(N,D)` 为 `[.20905,.54684]`，训练 RMSE `.049188`；constant-G 训练 RMSE `.056299`。这些全样本数值只作描述，不用于无偏选型。模型形式不确定性仍存在：若将来获得独立真实同口径数据、重复训练实验或 B8 机制解释，应重设检验并检验 Q×规模的真实可迁移性。

## 决策与局限

现有 v2 CHM 接口继续使用固定原版 B7 constant-G 诊断模型，不静默替换参数。交互候选在 B7 同源比较中有稳定的数值优势，保留为后续条件模型候选；`ready_for_Q3=false`，没有新总预测区间。B7 半合成、候选发明用过全部 B7、B8 方向冲突及 A/B 桥接不可识别均限制正式科学结论。若论文引用，只可在 B7 方法诊断小节明确这层限制，不称真实质量机制或真实外部预测。
