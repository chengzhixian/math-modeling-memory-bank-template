# B7 八参数联合拟合与两阶段对照

协议和来源见 `20260925-independent-protocol.md`。运行 `python -B src/cyj/fit_b7_joint_nonlinear.py --starts 12 --bootstrap 200`，代码起点 `af538e9a5db46b64ed02eb84b017f7738325171b`，Python 3.12.14、NumPy 2.3.5、SciPy 1.18.1。每次优化同时更新 E/A/B/alpha/beta/G0/GN/GD，未调用全样本指数作为折内初值。原 B7 在 `source_data` 内按清单核验，B6 精确子集只作重叠校验。

| 指标 | joint | staged |
|---|---:|---:|
| train RMSE | 0.04841013 | 0.04918831 |
| 留 N 平均折 RMSE | 0.04976351 | 0.05193559 |
| 留 D 平均折 RMSE | 0.04900752 | 0.05020663 |
| 留 Q 平均折 RMSE | 0.04935196 | 0.05006088 |
| 描述性 AIC | -2707.24162 | -2692.88935 |
| 描述性 BIC | -2670.25839 | -2655.90612 |

joint 参数依次为 `(1.7353861355,0.3575829483,1.2653732504,0.3392668091,0.3203945700,0.3701472577,-0.0594920642,-0.0158051979)`。12/12 全样本起点收敛到同一近最优盆地，未触参数搜索边界；扩大搜索界后变化量约 1e-8 至 1e-7，没有发现另一组等误差但明显不同的解。有限起点不证明全局唯一。四角最小 G=0.1941448352，保证整个 log 矩形支持域的质量单调性。

局部 Jacobian 满列秩 8，原单位条件数 46.45372、列归一条件数 49.13514。条件工作协方差中 corr(A,alpha)=-0.963976、corr(E,beta)=0.799303，说明部分系数难以分别作强解释。200 次 ND 簇 bootstrap 全部成功，分布、相关矩阵及 Gauss–Newton 矩阵见 `b7_identifiability.json`；这仅说明模型条件下数值可估计性，不能推成真实训练结构识别。

取舍：后续独立诊断和论文候选采用 joint，因为它完成了声明的同一八参数 SSE 优化、三轴留出均有改善、多起点与搜索界检查一致。该决定不只依赖训练 RMSE，也不以描述性 AIC/BIC 作独立验证。staged 与 joint 均显示域内 Q 的负 Loss 梯度；定量梯度与阈值将随参数变化，故后续实验重新运行。历史 `cyj.chm.v3@e36aa23` 保留原 staged 数字不改，新结果不能悄悄冒充其发布版。

局限：B7 半合成；函数族曾受全 B7 探索启发；没有真实独立训练测试；A/B 桥接未识别。`ready_for_Q3=false`。joint 输出 SHA256 `c7426036164238d41b92ca08e9ca9087224aeb0c74554478b66093774a5b5b1a`，原始 CSV 与 JSON 分别保留精确指标、全部起点和折级结果。
